# D07：模型一次运行到底发生什么

> **学习导航**：承接 D02 的 token 与向量、D05 的注意力和 D06 的完整 Transformer；本课用同一个预设请求串起应用层、推理引擎、模型前向、KV Cache 与逐 token 输出；完成后应能从用户文本开始，按顺序说出一次 `prefill` 和多次 `decode` 各自做了什么。

## 今日目标

能解释一条请求如何经过模板、分词、Embedding、Transformer、LM Head、解码策略和反分词变成文本；区分模型权重、当前上下文、KV Cache、logits、随机数与推理引擎的责任。

## 为什么要学这一课

只知道“模型预测下一个 token”仍然不够。它没有回答输入是谁整理的、整段提示怎样第一次进入模型、每层保存了什么、为什么第一个 token 慢、后续 token 又为什么逐个出现，也无法说明 vLLM 等引擎到底负责什么。本课把前面分散的概念接成一条真实运行链，后面的幻觉、部署、长上下文、量化和硬件瓶颈才能放回正确位置。

## 先分清三层

| 层 | 本次请求中负责什么 | 不负责什么 |
|---|---|---|
| 应用层 | 组织消息、系统提示、工具结果、权限与展示 | 直接执行 Transformer 矩阵计算 |
| 推理引擎 | 加载权重、组 batch、调度算子、管理显存与 KV Cache、执行解码 | 凭空给模型增加知识 |
| 模型 | 用固定权重把当前 token 序列变成 logits | 自动核验事实、决定业务权限或永久记住本次对话 |

下面所有编号和形状都是**教学示意**，不对应某个具体模型。不同模型的模板、词表、层数和隐藏宽度会变化，但责任链相同。

## 第一段动效：一条请求从文字走到文字

```pencil-flow
{
  "ariaLabel": "用户文字经过消息模板、分词、向量、Transformer、logits、token 选择和反分词，形成输出并把新 token 送回下一轮",
  "learningGoal": "能按顺序说出一次模型请求的八个阶段，并指出模型计算与应用处理的边界。",
  "watchFor": "盯住数据形态：文字变成 token ID，再变成浮点向量和 logits，最后才重新变回文字；权重始终只被读取。",
  "mode": "animated",
  "nodes": [
    { "id": "prompt", "label": "用户文字", "x": 78, "y": 58, "width": 118, "tone": "input" },
    { "id": "template", "label": "消息模板", "x": 252, "y": 58, "width": 126, "tone": "process" },
    { "id": "tokens", "label": "token ID", "x": 430, "y": 58, "width": 126, "tone": "change" },
    { "id": "embedding", "label": "Embedding 向量", "x": 646, "y": 58, "width": 168, "tone": "input" },
    { "id": "layers", "label": "多层 Attention + MLP", "x": 646, "y": 170, "width": 196, "tone": "process" },
    { "id": "logits", "label": "词表 logits", "x": 430, "y": 170, "width": 132, "tone": "process" },
    { "id": "choose", "label": "选择一个 token", "x": 252, "y": 170, "width": 154, "tone": "change" },
    { "id": "text", "label": "输出文字", "x": 78, "y": 170, "width": 118, "tone": "input" }
  ],
  "edges": [
    { "id": "format", "from": "prompt", "to": "template", "label": "组装" },
    { "id": "tokenize", "from": "template", "to": "tokens", "label": "分词" },
    { "id": "lookup", "from": "tokens", "to": "embedding", "label": "查表" },
    { "id": "forward", "from": "embedding", "to": "layers", "label": "前向" },
    { "id": "project", "from": "layers", "to": "logits", "label": "LM Head" },
    { "id": "sample", "from": "logits", "to": "choose", "label": "解码" },
    { "id": "detokenize", "from": "choose", "to": "text", "label": "反分词" },
    { "id": "loop", "from": "choose", "to": "tokens", "label": "追加后再算", "mobileHidden": true }
  ],
  "steps": [
    { "title": "应用先整理请求", "watch": "看原始文字先进入消息模板，还没有进入 Transformer。", "purpose": "把 system、user、assistant 角色与工具结果整理成模型约定的文本格式。", "detail": "模板属于应用或模型接口约定；同一句用户文字配上不同系统提示，会形成不同输入 token。", "reflection": "只改系统提示但不改权重，模型输入会不会变化？", "active": ["prompt", "format", "template"] },
    { "title": "文字变成模型输入", "watch": "看模板文本先变成整数 ID，再通过查表变成浮点向量。", "purpose": "把离散符号转换为神经网络能够计算的张量。", "detail": "token ID 只是词表编号；Embedding 向量才进入后续矩阵计算。不同 tokenizer 会切出不同数量的 token。", "reflection": "ID 842 本身能表示 842 倍的语义强度吗？", "active": ["template", "tokenize", "tokens", "lookup", "embedding"] },
    { "title": "固定权重完成前向", "watch": "看序列向量进入多层 Attention 和 MLP，位置数通常保持不变，隐藏表示不断更新。", "purpose": "让每个位置汇总可见上下文，并逐层形成适合预测下一 token 的表示。", "detail": "普通推理只读取训练好的权重，不执行反向传播和参数更新。推理引擎负责把这些算子正确、高效地放到硬件上执行。", "reflection": "一次普通聊天会因为做了前向计算就永久改变模型吗？", "active": ["embedding", "forward", "layers"] },
    { "title": "隐藏表示投影到词表", "watch": "看最后位置的隐藏向量通过 LM Head 变成一整列词表 logits。", "purpose": "为词表中的每个候选 token 给出一个未归一化分数。", "detail": "logits 不是事实表，也不是最终文字；softmax 与解码策略还要把候选分布压成一个 token。", "reflection": "最高 logit 的候选一定是事实正确答案吗？", "active": ["layers", "project", "logits"] },
    { "title": "选择、显示并进入下一轮", "watch": "在基础自回归路径中，看一列 logits 先接受一个 token；它被反分词显示，同时追加回 token 序列。", "purpose": "逐 token 形成回答，并让刚生成的内容参与下一轮预测。", "detail": "流式界面只是尽早显示已经接受的 token；基础路径按 token 循环，并不表示所有模型都没有隐藏思考或草稿阶段。推测解码、MTP 等实现可以一次提出多个候选，但仍需由目标模型验证后再接受。循环在停止 token、长度上限或应用规则触发时结束。", "reflection": "界面一次出现一整句话，能证明模型一次计算出整句话吗？", "active": ["logits", "sample", "choose", "detokenize", "text", "loop", "tokens"] }
  ]
}
```

## 用形状再走一遍

假设预设请求被切成 8 个 token，模型隐藏宽度为 4096，词表大小为 32000：

| 阶段 | 教学示意形状 | 轴的含义 |
|---|---|---|
| `input_ids` | `(1, 8)` | 1 个请求，8 个 token |
| 隐藏状态 | `(1, 8, 4096)` | 每个位置有 4096 个浮点特征 |
| 经过每个 Transformer Block | `(1, 8, 4096)` | 形状可不变，但数值和包含的上下文信息改变 |
| LM Head 输出 | `(1, 8, 32000)` | 每个位置都有一份词表分数 |
| 下一 token 候选 | `(1, 32000)` | 生成时通常读取最后一个位置的 logits |
| 选出的 token | 1 个整数 ID | 追加回序列，开始下一轮 |

这里的“多层”不是把 8 个 token 压成 1 个 token。模型通常保留位置轴，只在最后取适合下一步预测的位置；隐藏宽度与词表大小也不是同一个维度。

## 第二段动效：prefill 与 decode 为什么不同

```pencil-flow
{
  "ariaLabel": "输入 token 先做 prefill 并写入 KV Cache，随后每轮 decode 只处理新 token、复用历史缓存、选择并追加下一个 token",
  "learningGoal": "能区分 prefill 和 decode 的输入规模、缓存变化与性能瓶颈。",
  "watchFor": "第一次看整段提示，后续每轮主要看一个新 token；KV Cache 随已处理历史增长，模型权重不增长。",
  "mode": "animated",
  "nodes": [
    { "id": "input", "label": "8 个输入 token", "x": 94, "y": 58, "width": 150, "tone": "input" },
    { "id": "prefill", "label": "prefill 整段前向", "x": 290, "y": 58, "width": 166, "tone": "process" },
    { "id": "cache", "label": "各层 KV Cache", "x": 496, "y": 58, "width": 154, "tone": "change" },
    { "id": "first", "label": "首个 logits", "x": 680, "y": 58, "width": 126, "tone": "process" },
    { "id": "select", "label": "选择新 token", "x": 680, "y": 170, "width": 142, "tone": "change" },
    { "id": "append", "label": "追加到上下文", "x": 496, "y": 170, "width": 146, "tone": "input" },
    { "id": "decode", "label": "decode 单步前向", "x": 290, "y": 170, "width": 164, "tone": "process" },
    { "id": "stream", "label": "流式输出", "x": 94, "y": 170, "width": 126, "tone": "input" }
  ],
  "edges": [
    { "id": "read", "from": "input", "to": "prefill", "label": "整段读入" },
    { "id": "write", "from": "prefill", "to": "cache", "label": "写 K/V" },
    { "id": "predict", "from": "cache", "to": "first", "label": "最后位置" },
    { "id": "pick", "from": "first", "to": "select", "label": "解码" },
    { "id": "grow", "from": "select", "to": "append", "label": "追加" },
    { "id": "one", "from": "append", "to": "decode", "label": "只送新 token" },
    { "id": "reuse", "from": "decode", "to": "cache", "label": "复用并扩充", "mobileHidden": true },
    { "id": "show", "from": "decode", "to": "stream", "label": "输出" }
  ],
  "steps": [
    { "title": "prefill 处理完整提示", "watch": "看 8 个输入 token 一起经过模型，而不是逐字显示给用户。", "purpose": "建立所有输入位置的隐藏表示，并得到第一个输出 token 的 logits。", "detail": "输入越长，prefill 通常计算越多；首 token 时间 TTFT 还可能包含排队、检索、模板和冷启动。", "reflection": "输入从 100 token 增到 10000 token，首 token 延迟为什么可能明显增加？", "active": ["input", "read", "prefill"] },
    { "title": "把历史 K/V 写进缓存", "watch": "看缓存按层保存输入位置的 Key 和 Value，中间结果属于当前请求。", "purpose": "避免后续生成每个 token 时重复计算全部旧位置的 K/V。", "detail": "KV Cache 用显存换速度，会随层数、KV 头数、头维度、batch、精度和历史长度变化。删除缓存不会删除模型权重。", "reflection": "为什么两个并发请求通常不能直接共用同一份完整 KV Cache？", "active": ["prefill", "write", "cache", "predict", "first"] },
    { "title": "decode 通常先加入一个新 token", "watch": "在基础自回归路径中，看选出的 token 追加后，下一轮主要输入一个新位置，而旧位置从缓存读取。", "purpose": "用较少的新计算继续预测，同时保留对历史上下文的注意力访问。", "detail": "推测解码等方法可以提出多个候选并批量验证，所以“一个新 token”是基础教学路径，不是所有推理实现的硬性限制。decode 往往更受显存带宽、KV 读取、并发调度和小矩阵效率影响；它与 prefill 的瓶颈不完全相同。", "reflection": "KV Cache 为什么加速生成，却不会让模型突然知道更多事实？", "active": ["first", "pick", "select", "grow", "append", "one", "decode", "reuse", "cache"] },
    { "title": "流式显示并重复", "watch": "看单步结果发给界面，但循环仍继续，直到停止条件成立。", "purpose": "减少用户等待感，并持续生成后续 token。", "detail": "推理引擎负责调度这个循环；应用还可以在外层执行停止、内容过滤、工具调用和权限检查。", "reflection": "模型计算结束与产品决定停止回答，一定是同一个条件吗？", "active": ["decode", "show", "stream", "select", "append"] }
  ]
}
```

## 每轮会变和不会变的东西

| 对象 | 普通生成时是否变化 | 生命周期 |
|---|---|---|
| 模型权重 | 通常不变 | 跨请求复用，只有训练或加载新适配器才改变 |
| 当前 token 序列 | 每轮增加一个或多个系统插入的 token | 当前会话或请求 |
| KV Cache | 随已处理历史增长或被分页管理 | 当前请求，结束后可释放 |
| logits | 每轮重新计算 | 只描述当前下一步候选 |
| 采样随机数状态 | 使用随机采样时会推进 | 由 seed、采样算法和执行实现共同影响 |
| batch 与调度位置 | 可能随其他请求加入或结束而变化 | 推理引擎运行时 |

这张表解释了为什么“模型文件没变”不等于“每次输出必然相同”，也解释了为什么部署引擎会影响数值、随机性、延迟和吞吐，却不是模型知识的主要来源。

## 预设概率样例：怎样选一个 token

候选概率为：

| token | 概率 |
|---|---:|
| 蓝 | 0.50 |
| 晴 | 0.30 |
| 灰 | 0.15 |
| 雨 | 0.05 |

- **Greedy**：选“蓝”。
- **Temperature**：作用在 softmax 前的 logits 上；较低通常让分布更集中，较高通常让分布更平。下表展示的是已经归一化后的示意概率，不能直接把概率乘或除 temperature。
- **Top-k=2**：只保留“蓝、晴”，随后重新归一化。
- **Top-p=0.8**：按概率从高到低累计，保留达到 0.8 所需的最小候选集合（这里是“蓝、晴”），再对保留项重新归一化。

这些策略改变输出分布，不负责核验“蓝”是否符合事实。相同模型、相同上下文和相同解码参数在严格确定性实现中通常可以复现；使用随机采样，或实现存在数值非确定性（例如某些并行归约、精度和调度差异）时，结果可能变化。并发调度不是必然改变答案的原因，必须结合具体实现和日志核对。

## KV Cache 与固定状态不是一回事

标准注意力通常保留随序列增长的 Key/Value；线性注意力或状态空间类模块则可把历史压进固定形状的状态，单步状态内存不再随上下文线性增长。代价是压缩可能丢失细节，因此“固定状态”不等于“无损记住无限历史”。混合架构会让两类模块分工：大部分层承担低成本更新，少量标准注意力层补充精确检索能力。

要比较推理内存，应同时写清层数、状态或缓存形状、数据类型、batch 和上下文长度，不能只比较架构名称。KDA 的状态读写见 [外积与状态矩阵](../03-数学急救包/06-外积与状态矩阵.md)。

## 幻觉放在运行链的哪里看

训练目标与数据决定了参数里形成什么统计规律；本次上下文决定模型看到了什么；模型前向产生候选 logits；解码策略选出 token；应用层决定是否检索、核验、拒答或调用工具。幻觉可能由知识缺口、上下文冲突、推理错误、解码放大、检索失败或验证缺失共同造成，不能只归因于“随机采样”。

## 今日验收

<ExerciseBlock
  id="d07-greedy-decoding"
  type="qa"
  question="Greedy 每一步都选最高概率 token，为什么仍不能保证整段序列概率最高？"
  :concepts="[{ id: 'greedy-decoding', label: 'Greedy 解码', prerequisites: ['softmax-probability', 'lm-head'] }]"
  :misconceptions="[{ id: 'local-best-is-global-best', label: '把每步最高概率当成整段最优', explanation: 'Greedy 只做局部选择；当前略低的分支可能带来更高的后续联合概率。' }]"
  :remediation="{ href: '#预设概率样例-怎样选一个-token', title: '预设概率样例：怎样选一个 token', reason: '比较两条两步路径的条件概率乘积，分开局部选择和整段结果。' }"
  answer="因为当前最优选择可能把后续带入低概率路径，而另一个当前略低的选择可能拥有更高的后续联合概率。"
  :steps="['Greedy 只比较当前一步的候选概率。', '整段序列概率由每一步条件概率共同决定。', '局部最高的第一步不保证后续乘积最高，因此不等于全局搜索。']"
  :transfer="{
    question: '路径 A 首步 0.7、次步 0.2；路径 B 首步 0.6、次步 0.8。哪条两步联合概率更高？',
    options: ['A', 'B', '相同'],
    correct: 'B',
    explanation: 'A 为 0.14，B 为 0.48；首步较低的 B 整体更高。'
  }"
  mistake="确定性只表示同样输入通常得到同样输出，不表示全局最优或事实正确。"
/>

<ExerciseBlock
  id="d07-temperature"
  type="choice"
  question="其他设置不变时，提高 temperature 通常会怎样？"
  :concepts="[{ id: 'temperature', label: 'Temperature 采样', prerequisites: ['softmax-probability', 'greedy-decoding'] }]"
  :misconceptions="[
    { id: 'temperature-changes-weights-or-truth', label: '把温度当成改权重或事实校验', explanation: '温度只在本次解码中改变 logits 的相对尺度和采样分布，不会永久修改权重，也不会自动消除幻觉。', options: ['A', 'C', 'D'] }
  ]"
  :remediation="{ href: '#预设概率样例-怎样选一个-token', title: '预设概率样例：怎样选一个 token', reason: '对照低温和高温下同一组 logits 的概率集中程度。' }"
  :options="['让概率分布更集中', '让概率分布更平，低概率候选更有机会', '永久修改模型参数', '自动消除幻觉']"
  correct="B"
  answer="B。较高温度通常让分布更平，输出多样性增加。"
  :steps="['温度作用在 softmax 前的 logits 上。', '温度升高会缩小 logits 之间的相对差距。', 'softmax 后分布因此通常更平，更多候选获得可见概率。']"
  :transfer="{
    question: '想让输出更稳定、更偏向最高概率候选，通常应怎样调温度？',
    options: ['适当降低', '无限提高', '删除模型权重'],
    correct: 'A',
    explanation: '较低温度通常让分布更集中。'
  }"
  mistake="温度改变选择分布，不负责核验候选内容是否真实。"
/>

<ExerciseBlock
  id="d07-top-k-renormalization"
  type="calculation"
  question="只保留概率 0.50 和 0.30 的两个 top-k 候选并重新归一化，它们的新概率分别是多少？"
  :concepts="[{ id: 'top-k-sampling', label: 'Top-k 截断与重归一化', prerequisites: ['softmax-probability'] }]"
  :misconceptions="[{ id: 'top-k-skips-renormalization', label: '截断后仍直接使用原概率', explanation: '删除其他候选后，保留概率的总和通常小于 1，必须除以保留项总和重新归一化。' }]"
  :remediation="{ href: '#预设概率样例-怎样选一个-token', title: '预设概率样例：怎样选一个 token', reason: '先算保留项总和，再让每项除以这个新分母。' }"
  answer="新概率分别是 0.625 和 0.375。"
  :steps="['保留项的原概率和是 0.50 + 0.30 = 0.80。', '第一项重新归一化为 0.50 ÷ 0.80 = 0.625。', '第二项为 0.30 ÷ 0.80 = 0.375，两项之和重新等于 1。']"
  :transfer="{
    question: '保留概率 0.6 和 0.2 两项后，第一项的新概率是多少？',
    options: ['0.6', '0.75', '0.8'],
    correct: 'B',
    explanation: '保留项总和 0.8，0.6 ÷ 0.8 = 0.75。'
  }"
  mistake="不能保留原概率 0.50 和 0.30 后直接抽样，否则总和只有 0.80。"
/>

<ExerciseBlock
  id="d07-prefill-decode"
  type="qa"
  question="prefill 和 decode 分别处理什么，为什么两者性能瓶颈可能不同？"
  :concepts="[{ id: 'prefill-decode', label: 'Prefill 与 Decode', prerequisites: ['transformer-flow', 'causal-mask'] }]"
  :misconceptions="[{ id: 'decode-ignores-history', label: '认为 Decode 不再读取历史', explanation: 'Decode 不会重算全部历史 token，但会通过 KV Cache 读取历史 K/V。' }]"
  :remediation="{ href: '#第二段动效-prefill-与-decode-为什么不同', title: '第二段动效：prefill 与 decode 为什么不同', reason: '分别观察整段输入并行计算与每轮新增一个 token 的执行形态。' }"
  answer="prefill 处理完整输入并建立缓存；decode 每轮主要处理新 token 并读取历史缓存。前者更受输入规模和大矩阵计算影响，后者常更受 KV 读取、带宽和调度影响。"
  :steps="['prefill 一次接收整段提示 token。', '它为输入位置计算并保存各层 K/V，同时产生首个输出 logits。', 'decode 复用旧 K/V，只为新位置继续计算，因此计算形态和主要瓶颈不同。']"
  :transfer="{
    question: '长提示刚送入模型、尚未生成第一个新 token，此时主要处在哪个阶段？',
    options: ['Prefill', 'Decode', '训练反向传播'],
    correct: 'A',
    explanation: '完整提示的首次处理和缓存建立属于 prefill。'
  }"
  mistake="decode 不是完全不看历史，而是通过 KV Cache 读取历史。"
/>

<ExerciseBlock
  id="d07-weights-vs-kv-cache"
  type="choice"
  question="关于模型权重与 KV Cache，哪项最准确？"
  :concepts="[{ id: 'weights-vs-kv-cache', label: '模型权重与 KV Cache', prerequisites: ['prefill-decode'] }]"
  :misconceptions="[
    { id: 'kv-cache-is-model-knowledge', label: '把 KV Cache 当成模型知识或参数', explanation: '权重跨请求保存训练所得计算规则；KV Cache 是当前请求根据历史 token 临时算出的中间结果。', options: ['A', 'C', 'D'] }
  ]"
  :remediation="{ href: '#kv-cache-与固定状态不是一回事', title: 'KV Cache 与固定状态不是一回事', reason: '按来源、生命周期和作用对照权重与缓存。' }"
  :options="['两者都在每轮永久增长', '权重跨请求复用，KV Cache 属于当前请求的中间结果', '删除 KV Cache 会删除模型知识', 'KV Cache 决定 tokenizer 怎样切词']"
  correct="B"
  answer="B。模型权重决定计算规则，KV Cache 保存当前请求已经算出的历史 K/V。"
  :steps="['权重在普通推理时通常只读，并跨请求复用。', 'KV Cache 由当前 token 序列计算得到，会随请求历史变化。', '请求结束释放缓存不会改变模型文件或训练所得知识。']"
  :transfer="{
    question: '一次请求结束并释放 KV Cache 后，哪项仍然保留？',
    options: ['模型权重', '该请求的全部 K/V 中间结果', '刚才采样用的随机数状态必须永久保存'],
    correct: 'A',
    explanation: '权重属于模型本身，当前请求缓存可以释放。'
  }"
  mistake="KV Cache 能加速历史复用，但不是另一份模型参数。"
/>

<ExerciseBlock
  id="d07-kv-cache-size"
  type="calculation"
  question="简化 KV Cache 有 2 层、4 个 KV 头、每头维度 8、10 个历史 token，并同时保存 K 和 V。共有多少个数值？若每个数值 2 bytes，占多少 bytes？"
  :concepts="[{ id: 'kv-cache-size', label: 'KV Cache 容量估算', prerequisites: ['weights-vs-kv-cache', 'multi-head-shape'] }]"
  :misconceptions="[{ id: 'kv-cache-size-misses-factor', label: '容量公式漏乘层、头、K/V 或字节数', explanation: '容量要依次乘层数、KV 头数、每头维度、历史 token 数、K/V 两份和每个数值字节数。' }]"
  :remediation="{ href: '#kv-cache-与固定状态不是一回事', title: 'KV Cache 与固定状态不是一回事', reason: '把容量拆成“一个 K 或 V”→“K 与 V 两份”→“换算字节”三步。' }"
  answer="共有 1,280 个数值，占 2,560 bytes。"
  :steps="['单个 K 或 V 的数值数是 2 层 × 4 头 × 8 维 × 10 token = 640。', 'K 和 V 各一份，所以 640 × 2 = 1,280 个数值。', '每个数值 2 bytes，因此 1,280 × 2 = 2,560 bytes。']"
  :transfer="{
    question: '1 层、2 个 KV 头、每头 4 维、5 个 token，同时保存 K/V，共有多少个数值？',
    options: ['40', '80', '160'],
    correct: 'B',
    explanation: '1 × 2 × 4 × 5 × 2 = 80 个数值。'
  }"
  mistake="真实服务还要乘 batch，并考虑布局、对齐和其他工作区开销。"
/>

下一课：[D08：训练数据与分词器](D08-训练数据与分词器.md)
