# D03：够用就好的数学基础

## 今日目标

只用加减乘除理解向量、点积、概率、平均 loss 和参数更新。

## 一次完整的小计算

两个二维向量：

```text
Query = [1, 2]
Key   = [3, 1]
```

点积是 $1\times3 + 2\times1 = 5$。它把两排数压成一个匹配分数。注意力会对多个 Key 得到多个分数，再转成权重。

假设两个候选 token 的 logits 是 `[0, 1]`。softmax 后约为 `[0.27, 0.73]`。如果第二个 token 是正确答案，单位置交叉熵约为 $-\ln(0.73) = 0.315$。

现在某个参数是 `2.0`，梯度是 `0.4`，学习率是 `0.1`：

$$
\text{新参数} = 2.0 - 0.1\times0.4 = 1.96
$$

这条链已经覆盖训练的数学骨架：

```text
向量计算 -> logits -> 概率 -> loss -> 梯度 -> 更新参数
```

## 五个符号翻译

| 符号 | 读法 | 朴素含义 |
|---|---|---|
| `Σ` | 求和 | 把一组数加起来 |
| `×` 或 `·` | 乘或点积 | 组合数字得到新数 |
| `p` | probability | 某候选的概率 |
| `log` | 对数 | 把概率变成适合累加的惩罚尺度 |
| `∇` | 梯度 | loss 对各参数的变化方向和敏感度 |

需要补课时分别查看 [向量与点积](../03-数学急救包/02-向量、矩阵与点积.md)、[概率与 softmax](../03-数学急救包/03-概率与softmax.md)、[梯度](../03-数学急救包/04-导数、梯度与学习率.md) 和 [交叉熵](../03-数学急救包/05-对数与交叉熵.md)。

## 不要掉进三个坑

- 向量的某一维通常没有预先命名的人类含义。
- 训练 loss 是许多位置和样本的汇总，单个数字看不出所有失败模式。
- 梯度告诉局部变化，不能保证一步就走到全局最好位置。

## 今日验收

手算：

1. $[2, 3] \cdot [4, 1]$
2. 参数 `5`、梯度 `-2`、学习率 `0.1` 的新参数
3. 三个 batch loss `1.2、0.9、0.6` 的平均数

答案：`11`；`5.2`；`0.9`。

下一课：[D04：神经网络如何学习](D04-神经网络如何学习.md)
