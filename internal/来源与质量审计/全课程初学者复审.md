# 全课程初学者复审

复审日期：2026-08-11。范围包括 `course/` 下 189 篇 Markdown，以及理论基础、训练过程案例、专题课程、论文研读、数学急救包、图解材料、速查表和兼容入口；仓库其余 Markdown 同步检查本地链接与结构。复审目标不是继续增加术语，而是检查初学者能否把知识接起来、算出来、说出来并在预设案例中认出来。

## 七个审查问题

每一页都从下面七个角度检查：

1. **先修关系**：正文是否使用了尚未解释的对象、符号或代码动作？
2. **概念深度**：是否只给一句类比，还是说明了输入、动作、输出与边界？
3. **具体到抽象**：是否先用具体数字完成计算，再压缩成公式？
4. **标题闭环**：小例子结尾是否明确回答标题提出的主张？
5. **上下文联系**：是否说清从上一课带来什么、下一课会拿它做什么？
6. **学习证据**：学完后能否计算、判断、画图、排错或产出文件，而不只是觉得“看懂了”？
7. **视觉必要性**：图或动效是否帮助定位、跟随变化和立即回忆，而不是只增加装饰？

## 主要问题与处理

| 共性问题 | 初学者受到的影响 | 本轮统一处理 |
|---|---|---|
| 页面各自正确，但课与课之间缺少接力 | 记得词，却不知道新知识从哪里来、要送到哪里 | 理论基础改为“总纲 -> 放大课 -> 回到总纲”，六段主线共用 L0，并保留每课学习导航 |
| 目标常写成“理解” | 无法判断自己是否真正掌握 | 改成能画、能算、能解释、能设计、能排错等可观察动作 |
| D03 一天承担过多数学概念 | 容易把“今天没有全背会”误认为不适合学习 | 标出第一遍必修线与随后加深项；保留小数字到通式的路径 |
| 符号和公式一次压缩过多动作 | 会读出公式，却不知道每个符号指什么、从哪一步得到 | 统一先做小算式、对象与形状表，再给通式；补数学下标与 Python 索引 |
| 抽象标题没有回到例子 | 算完数仍不明白“矩阵映射空间”等句子 | 区分空间、具体向量、矩阵规则和输出，并在例子末尾回扣标题 |
| 图解容易被当成浏览材料 | 看完有印象，但无法主动调用 | 图解页增加看前预测与看后立即回答；动效保留反思问题和总览 |
| 理论到实战交接过快 | 会背 Transformer 图，却认不出真实项目的尺度和证据 | 生命周期总纲区分项目、训练任务和 step；D14 补线上迭代，D15-D21 延续预设产物链 |
| 数学急救包深浅不均 | 某些页面只有定义，缺少“会用”的证据 | 01-05 补使用位置、数值例子和立即自检；06-08 标明拓展边界 |
| 高级内容与必修内容混在一起 | 容易把论文名词误当成入门门槛 | D12、数学 06-07 和 K3 深读明确“先抓共同结构、推导可后学” |
| 兼容入口解释更名、重组和旧链接 | 学习者先看到站点维护历史，不知道当前该打开哪一课 | 4 个入口改为直接给出当前课程或基础闭环路线，并增加回归检查 |
| 共用教学模型的完整配置在两页遗漏 `D_h` | 学习者无法从 `D=4、H=2` 直接核对每头维度，且会误以为页面使用不同模型 | 补齐理论课首页和训练总纲的 `D_h=2`，并让三张总纲与理论课首页共用回归标记 |
| 左侧目录按 URL 切成多套局部侧栏 | 进入理论课后看不到案例、专题和查阅工具，进入论文页又失去基础课程位置；用户无法从左侧建立完整课程地图 | 合并为所有页面共用的全站课程树；六个一级入口始终存在，当前分支自动展开，理论主线、专题与论文系列保留二级分组，其他分支可折叠 |
| tiny-llm 冻结证据按原始 CRLF 字节统计字符，并引用不含实现的提交 | 数据规模、切分算术和代码快照无法复查，模型卡的复现声明失真 | 按 Python 文本读取口径统一为 1,288 字符、1,159/129 切分，改用真实提交 `95d627307a99`，补参数量明细与回归检查 |
| 已填充的数据卡、模型卡审查框架仍被旧规则排除在侧栏之外 | 页面虽然发布，却没有站内入口，学习者无法从 D16/D21 附近发现 | 把两张审查卡插入同一个连续展开的训练案例目录，并增加顺序回归检查 |
| 侧栏已插入审查卡，但案例首页和正文“下一课”仍沿用旧顺序；数据卡版本只写“随课程冻结” | 学习者会从 D16 直接跳到 D17，pager 却先进入数据卡；审查卡自身也无法定位到精确语料版本 | 首页、正文链接与侧栏统一为同一顺序；数据卡绑定提交和源文件，并增加正文接力回归检查 |
| 数据卡与模型卡的三列表格在 390px 窄屏把审查列压成单字竖排 | 页面没有整页溢出，但阅读高度和逐字段对照成本异常增大 | 两张审查卡使用独立页面标记；窄屏按字段纵向展示冻结记录与审查重点，桌面和其他课程表格不变 |
| 用户反馈 D08 进入训练主线后明显变浅，只剩常识、分类和注意事项 | 学习者知道 LoRA、DPO、RAG、SLO 等名词，却算不出信号与资源，也无法从失败证据定位责任层 | D08-D14 统一补“对象或状态 -> 可手算账本 -> 失败证据 -> 方法边界”：D08-D10 深挖 token、更新、显存与并行；D11-D14 深挖适配器、偏好信号、推理系统与线上统计，并加入深度章节回归检查 |

仓库文档复核覆盖 208 个 Markdown，修正了内部审计文档中 13 个仍按旧目录层级指向课程的相对链接，并统一了 L0 和 tiny-llm 的冻结证据。本地链接、H1、标题层级、空章节和图片替代文本复查通过；根目录规则文档、内部文档链接、共享配置、案例数据统计和目录展开状态已纳入 `docs:check`。

## 主线能力链

| 阶段 | 页面 | 学完应留下的能力 |
|---|---|---|
| 模型原理 | 原理总纲、D01-D04 | 追踪文本、token、ID、向量、logits、概率、loss 与参数更新 |
| 模型架构与运行 | 架构总纲、D05-D07 | 沿模型 -> Block -> 子层 -> 矩阵追踪形状，并解释逐 token 生成 |
| 数据准备与模型训练 | 生命周期总纲、D08-D10 | 区分项目、训练任务和 step，读日志并理解规模化训练 |
| 模型评估与优化 | D11-D12 | 选择后训练方式，并用独立评测和回归证据约束结论 |
| 推理、部署与应用 | D13 | 解释推理服务、RAG 与 Agent 的责任边界 |
| 监控、反馈与持续迭代 | D14 | 区分四层监控、定位漂移与故障，并控制反馈回流、发布和回滚 |
| 案例闭环 | D15-D21 | 解释基线、检查数据、读张量形状、沿日志排错、比较对照并审查模型卡 |

若某一阶段只能复述名词，不能完成右列动作，应回到对应页面的“学习导航”和验收，不建议仅靠继续向后阅读解决。

## 动效取舍

本轮没有把所有重点都改成动画。只有三类知识保留或新增状态变化：表示形式沿流程变化、向量位置与汇总过程、生成上下文逐轮增长。矩阵映射、对照表、公式单次代入和固定系统结构使用静态图或表格，因为播放不会增加新的学习信息。

详细逐项判断见[教学动效审计](教学动效审计.md)。

## 仍需持续复查

- 模型、硬件、前沿架构和排行榜会变化，相关数字必须保留来源、日期和适用条件。
- 初学者难点会随真实使用继续暴露；新增解释应优先修复共性压缩，而不是在每页复制一套更长定义。
- 每次新增主线术语、公式或动效，都应重新回答本页的先修关系、完成证据和使用边界。

自动检查会保持基础闭环模块的学习导航、理论课每课的 6 道验收题、Wiki 锚点与动效结构不被后续修改破坏。人工复审仍负责判断解释是否真正连贯，自动规则不能替代初学者试读。
