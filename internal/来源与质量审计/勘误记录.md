# 勘误记录

| 日期 | 位置 | 问题 | 修订 |
|---|---|---|---|
| 2026-08-13 | D14 多区域时间型 SLO | 用“任一区域探测失败则不可用，否则可用”的二分句式，把未发出探测、遥测记录缺失等未知状态落入了“可用”分支，与后文“监控缺失是无法判断”冲突 | 明确只有所有指定区域均取得有效结果且全部成功才记为可用；预定失败与超时记为不可用；未探测或记录缺失按预先政策保守计为失败或把窗口标为不可判定，并增加自动回归检查 |
| 2026-08-13 | D14 非劣判断、事故调查、确认测试与系统恢复 | 把“区间包含 0 不能证明等价”错误扩展成不能证明非劣；把连续窗口确认写成根因调查前置条件；又把未参与版本选择近似当成测试独立性的充分条件，遗漏训练污染、近重复、公开泄露和访问历史；恢复表只覆盖模型、配置和接口，遗漏数据库迁移、队列状态和已发生的外部副作用 | 明确非劣看方向正确的单侧界限是否越过预设界值，包含 0 本身既不证明也不排除非劣；高损害事件并行止损、核验和调查；确认性证据同时检查训练参与、重复与泄露；恢复方案区分可回滚组件、前向修复和支付、通知等补偿动作，并增加自动回归检查 |
| 2026-08-13 | D14 四层监控、发布流程、测试隔离与反馈练习 | 把“不能只看总分”写成综合分本身不可使用，把灰度发布写成所有变更的必经步骤，把冻结测试集写成永远不得转为训练材料；反馈练习还默认要求关联原请求、检索和完整工具轨迹，与正文的数据最小化原则冲突 | 明确综合分只能辅助摘要或排序，分层、切片与安全硬门槛必须保留；把影子、灰度、蓝绿、隔离验证和人工审批改为按风险选择的发布工具，并说明蓝绿流量切换不自动覆盖数据库与外部依赖；限定测试隔离只在测试集承担独立确认职责期间成立，数据许可与用途允许且另建独立测试集后才可把旧题转为开发材料，此后失去未见测试身份；练习答案改为只关联获准处理的必要字段 |
| 2026-08-13 | D14 比例区间、概率抽样与反馈证据 | 把“样本只覆盖低风险子总体”的外推偏差误写成独立同分布失效，混淆随机误差与选择偏差；正态近似条件被简化成样本量和比例位置；概率抽样遗漏目标单位必须有非零入选概率；反馈章节又在强调数据最小化的同时默认关联完整上下文与轨迹 | 分开子总体内标准误、目标总体错位和相关请求三类问题；改用预期成功数与失败数说明正态近似诊断；明确每个待推断单位须有已知且大于 0 的入选概率；正文与练习统一为按核验目的保留获准处理的必要字段，只在定位对应链路时调取检索或工具证据，并增加自动回归检查 |
| 2026-08-13 | 概率与 Softmax、全局术语速查 | 页面直接使用“取指数”和 `e^z`，却没有区分右上角的幂指数、指数运算与结果，初学者也无法解释 `e^0`、`e^-1` 或负幂指数为何仍产生正权重 | 分别新增 `exponent（幂指数）` 与 `exponentiation（指数运算）` 全局词条、稳定锚点、沟通例句、关系图和离线发音；普通“指数”因还可能表示成本指数、浮点指数位等，不设为自动别名；Softmax 页增加 `e^0`、`e^1`、`e^2`、`e^-1` 小账本，限定重复相乘只适用于正整数幂指数，并补一般实数幂的定义域边界与“指数权重尚非概率”提醒 |
| 2026-08-13 | D14 比例标准误、漂移、安全控制、影子流量与反馈回流 | 把已知真实比例下的理论标准误写成近似式，没有区分用样本比例代入的估计误差与正态区间近似；漂移表容易被误读成互斥分类；又把所有拒答语义都暗示为可由应用确定性识别，遗漏开放文本检测误判、影子流量的数据处理边界、人工裁决分歧和恶意重复反馈；故障记录还要求每条默认关联全部权限与轨迹字段，违背数据最小化，练习用含糊的“随机且有代表性”代替概率抽样设计 | 分开理论标准误、估计标准误与粗略区间；用 `P(X)` 与 `P(Y|X)` 写清漂移且说明可共存；只对身份、资源、动作等可判定边界承诺执行点强制阻断，语义检测保留误判与分层防护；为影子流量补用途、许可、地域、保留、外部服务与副作用边界，为反馈补多人一致性、分歧保留和恶意来源溯源；失败字段改为按调查目的最小化，练习改用已知概率机制与设计型分析；同步增加自动回归检查 |
| 2026-08-13 | D14 回归门禁、灰度推断、时间型 SLO 与总体抽样 | 把所有抽样外推都写成依赖模型假设，忽略已知入选概率下可按抽样设计推断；按原始分配分析又被笼统称为实际版本效果；重复回归集的能力边界、随机实验缺失结局、多指标与多切片误报、离散探针怎样形成不可用时长均未讲清；又把 `P99` 延迟写成“P99 超时”，并未限定人工裁决后的 `0.5%` 描述性下限成立条件 | 区分概率抽样与非概率筛选、分配策略效果与实际版本暴露效果；明确回归集只守住已收录问题，缺失结局需预案并比较合理处理方式，多项探索不得冒充确认性结论；把 43.2 分钟改为一分钟格口径并补探测频率、离散化误差和秒级计量边界；补充普查不消除测量错误，修正 P99 与已知严重问题下限的表述，并增加自动回归检查 |
| 2026-08-13 | D14 线上监控、SLO、灰度与风险抽样；术语速查 | 将 99.9% 可用性默认换算为 43.2 分钟且未定义时间型 SLO 的测量端、服务范围与好坏判定；监控缺失被列为普通排除项；灰度把请求默认视为独立样本，遗漏按原始分配分析与估计对象，双侧区间又被直接用于非劣结论；告警越线被写成确认故障扩大；总体发生率、随机化版本比较与红队覆盖混用概率抽样前提；又把三类证据都要求与开发隔离；风险过采样没有限定目标总体和层内随机；冻结测试集、回滚数据时效与红队样本边界不完整；拒答与一般偏好混写，模型能力不足与偏好问题没有分开 | 绑定错误预算的指标、分母、测量范围和单位，监控缺失改为状态未知并要求保守处置；补随机化和分析单位、原始分配、估计对象、单侧非劣口径、序贯查看、告警确认、分层方差与测试集失效条件；拆开总体估计、随机化因果比较和红队覆盖，并允许开发证据保留其历史用途；明确一致回滚不回退有效事实；强制拒答与权限交给确定性控制，区分任务能力、SFT 与偏好优化职责，并增加自动回归检查 |
| 2026-08-13 | 动画模型实验室、D07 生成后处理与停止控制 | 将 EOS、长度上限、停止字符串和业务规则统一归为“应用层停止判断”，并把已接受 ID 记录、下一轮模型输入与可见文本缓冲混称为逻辑序列；总览的 decode 返回仍只有文字提示，没有真正连接第 05 步 | 分开生成循环的 EOS/长度控制、文本缓冲的停止字符串匹配和应用规则；明确已接受 ID 可记录 EOS，但只有未停止序列才把 ID 作为下一模型位置；总览用实际 SVG 橙色回边连接 `Decode 单步前向` 与第 05 步 |
| 2026-08-13 | 动画模型实验室生成路线图 | “未停止才进入下一轮 decode”只是一条带 `↩` 的说明，没有可见的 decode 运算节点或回到 logits 选择的终点，初学者可能误读为再次整段 prefill | 将条件分支改为橙色 `Decode 单步前向` 卡片，明确 `(1,1,D) + 历史 K/V → (1,V) 原始 logits`、只算新位置且不重做整段 prefill，并以可见回边连接到第 05 步的 ID 选择；内容规范与自动检查同步约束 |
| 2026-08-13 | D07 三组运行动效、全局知识图谱 | 将 `(B,V)` 压成整个 batch 只产生 1 个 ID，且多张图对已接受 ID 是否显示、是否继续的口径不一致；全局图还用 `position → embedding` 固定表达所有位置机制 | 改为每条活跃序列各接受 1 个 ID，即 `(B,V) → (B,)`；ID 先经应用层缓冲和逐序列停止判断，显示可为 0 字符，未停止才继续；补回 `token ID → Embedding` 查表路径，并区分位置 Embedding、RoPE 与相对位置偏置的接入点；自动检查禁止旧形状和旧路径 |
| 2026-08-12 | 全站 246 个 Wiki 术语、正文悬停预览与术语速查 | 术语只有定义和误解提醒，学习者知道“是什么”后仍不会在实验讨论、方案评审和排错记录中准确使用 | 为全部术语增加唯一来源的沟通例句；桌面悬停、键盘聚焦、手机点击与术语速查同步展示；内容检查强制例句非空、完整、非定义复述且不是“X 很重要”类空句 |
| 2026-08-12 | 全站 68 个块级公式、D05 注意力机制、数学急救包与公式关系图组件 | 公式虽已补构造理由与删项后果，但主要依赖正文和定义表；学习者仍难追踪谁从哪里来、谁与谁运算、数字怎样进入、结果怎样出来。外部招聘类比还把 Value 写成未来业绩，容易误导为模型读取尚未发生的信息 | 新增 `formula-story` 的顺序、分叉、汇聚三类关系图；为全部块级公式补日常身份、真实动作连线、手算数字、删项反例和边界；D05 用同一隐藏状态分出 Q/K/V，并把 Value 修正为当前被加权读取的内容；内容检查强制公式覆盖并校验图数量、结构和覆盖额度 |
| 2026-08-12 | D01、D10-D11、D14、小模型蒸馏、在策略蒸馏与软硬件瓶颈 | 第二层公式大多给出了正确结果，但没有解释条件概率删项、`6ND` 常数来源、LoRA 零初始化退化、标准误差、蒸馏负号、top-k 分母、KV 各存储轴、算术强度内存边界和自回归条件不可删除的原因 | 补齐构造依据、参数职责、错误替换与单位边界；总纲只回链数学母页；把关键证据加入内容检查，未引入新公式或新算法 |
| 2026-08-12 | 数学急救包、D04-D05、公式速查与 24 篇模型算法图解 | 多处公式只给定义、流程或用途，没有说明构造理由、参数职责与删项后果；算法图解规范还禁止最小公式推导，容易让学习者把设计动机误当实验结论，也没有隔离原创公式的评审入口 | 为比例、矩阵映射、Softmax、梯度下降、交叉熵、注意力缩放、状态擦写、负载与平滑封顶补推导和反例；24 篇算法图解逐篇增加部件反事实；区分数学证明、机制反例和实验效果；新增创新待评审隔离清单与内容检查 |
| 2026-08-12 | D07：模型一次运行到底发生什么，两段运行流程动效 | 主运行地图和 prefill/decode 动效把已接受 token 直接连到显示与下一轮 decode，未表达 EOS、停止序列、长度上限、内容过滤、工具调用或权限规则可能阻断下一轮；这会把基础路径误读成无条件循环 | 两张图统一改为“已接受 ID → 应用层处理 → 显示 / 停止条件判断”；命中停止条件进入结束，只有未命中才追加上下文并进入 decode；增加 D07 回归检查，禁止旧的无条件边 |
| 2026-08-12 | 动画模型实验室、D07 的 KV Cache 与 logits 路径 | 用 `L` 同时暗示层数或逻辑序列长度、用 `T` 同时暗示提示长度或缓存长度；“最后有效位置 → logits”还可能让初学者误以为 Transformer 主干只计算一个位置；解码边把 greedy、temperature、top-p 写成同级互斥项；分步动画在停止判断尚未完成时提前点亮 decode | 统一用 `N_layer`（层数）、`S_cache`（缓存位置数）、`T_prompt`（prefill 有效提示长度）；明确主干先计算全部有效提示位置的隐藏状态，生成时通常只取最后有效位置送入 LM Head；把边标签改为“应用约束、分数处理与选择规则”；应用层步骤只到停止判断，下一步才进入“未停止 → 追加到逻辑序列 → decode”分支，并增加结构化回归检查 |
| 2026-08-12 | 动画模型实验室、解码策略术语 | 将 next token 写成“下一个词”，把位置信息统一写成加入 Embedding，并把 logits、概率、temperature、top-k/top-p、greedy 与采样混成同一步；又把 `(B,T,V)`、KV Cache 增长和检查点读取写成所有实现每轮必然发生，还把已接受 ID 的显示与继续画成二选一，忽略 padding 后“最后有效位置”不一定是张量末列 | 按带 KV Cache 的 Decoder-only Transformer 基础自回归路径重画：区分原始 logits、采样处理与最终选择；主图固定 `B=1`，把全位置投影 `(1,T,V)` 与生成所需 `(1,V)` 分开，另标出批处理定位边界；从同一已接受 ID 用实线分出增量显示和停止判断，只让“未停止”结果进入 decode 回环；不将终止 token 是否记入逻辑序列写成统一顺序，避免与 D07 表示层数的 L 混淆，并同步修订术语、内容规范与回归检查 |
| 2026-08-12 | 模型评测榜单：Arena、LiveBench、SWE-bench 与检查脚本 | Arena 的 rank spread 被压成确定第 1–5 名；LiveBench 隐藏 3 个类别却未解释 Overall 的 7 类聚合；SWE-bench 同分被拆成第 2、第 3；脚本没有校验成本、开放状态与条件字段 | Arena 改显示官方名次区间，SWE-bench 改竞赛并列名次，LiveBench 写明总分组成与不可复算边界；补全模型身份，移除筛选后的伪排名，证据与脚本开始逐项校验机构、开放状态、成本、显示值、条件和位置 |
| 2026-08-12 | 模型评测与选型：指标表、动态榜单快照 | 将 WER 写成“语音识别准确率”，并混写 Recall@k 与 Hit@k；动态榜单只保存访问日和链接，后来又把规范化记录哈希误当官方来源证据 | WER 改为 `(替换 S + 删除 D + 插入 I) / 参考词数 N` 并注明可超过 100%，拆开 Recall@k 与 Hit@k；新增逐源逐行记录，明确当前没有不可变官方源工件，脚本仅校验页面与仓库记录一致 |
| 2026-08-11 | 全站左侧目录 | 侧栏按 URL 替换为理论、案例、专题或论文等局部目录，用户进入内容后看不到其他课程区域；仅调整理论课内部折叠状态不能解决全局定位问题 | 合并为所有页面共用的一棵全站课程树，固定显示六个一级入口；当前分支自动展开，其他分支可折叠并在站内跳转时保留状态，内部继续使用传统树形二级分组 |
| 2026-08-11 | 数据卡、模型卡审查框架移动端 | 三列表格在 390px 下把第三列压成接近单字宽，虽无整页横向溢出但无法高效逐字段阅读 | 为两张审查卡增加局部窄屏布局，按字段纵向展示冻结记录和审查重点；桌面及其他课程表格不受影响 |
| 2026-08-11 | 训练过程案例首页、D16/D21、数据卡与模型卡 | 侧栏恢复两张审查卡后，首页阅读顺序和正文“下一课”仍跳过它们；数据卡版本只写“随课程版本冻结”，无法定位精确语料 | 统一首页、正文和侧栏顺序；D16/D21 明确进入配套审查卡，数据卡绑定提交 `95d627307a99` 与源文件，并增加接力回归检查 |
| 2026-08-11 | 训练过程案例侧栏、仓库级内容检查 | 已填充的数据卡和模型卡审查框架仍被旧规则当作空模板排除，且仓库级 Markdown 链接检查漏掉根目录 `AGENTS.md` | 两张审查卡按 D16/D21 的学习位置插回同一个连续侧栏；检查范围补齐为 189 篇课程、208 篇仓库 Markdown，并增加目录顺序回归检查 |
| 2026-08-11 | D15-D21、tiny-llm 数据卡与模型卡 | 把 Windows 工作区 CRLF 中的 43 个 `\r` 计入语料，写成 1,331 字符和 1,197/134 切分；模型卡还引用了不含 `tiny-llm` 的提交 `7220daf58dc4` | 按 Python 文本读取口径改为 1,288 字符和 1,159/129 切分，区分 417 个源字符与含 `<unk>` 的 418 词表，代码快照改为 `95d627307a99`，补参数复算与自动检查 |
| 2026-08-11 | 课程首页、理论基础侧栏 | 开始页已连续展开，但首页“按需查阅”和理论课六段主线仍默认折叠，进入课程后看不到全部目录 | 默认展开全部一级入口和 D01-D14；超长论文深读层级保留按需折叠，并增加目录状态检查 |
| 2026-08-11 | 理论课首页、模型全生命周期总纲 | 两处完整列举 L0 配置时遗漏每头维度 `D_h=2`，与原理总纲、架构总纲不一致 | 补齐 `D_h=2`，并为三张总纲和理论课首页增加统一配置回归检查 |
| 2026-08-11 | 6 份内部来源与质量审计文档 | 13 个课程链接仍按旧目录层级解析，点击会落到不存在的 `internal/...` 路径 | 全部改为从仓库根进入 `course/` 的有效相对路径，并让内容检查覆盖内部文档链接 |
| 2026-08-11 | 21 天路线入口、D07/D09/D14 兼容入口 | 页面向学习者解释“已更名、已重组、旧书签或旧链接兼容”等维护历史，没有直接聚焦当前学习动作 | 保留稳定路径和自动跳转，正文只提供当前课程或基础闭环路线；增加兼容入口回归检查 |
| 2026-08-10 | D04-D14、后训练、多模态、AI Infra、模型量化 | 抽样统计分母、正确率、梯度适用范围、案例数据规模、PTQ 校准条件和显存单位存在错误或边界不清 | 区分样本比例与总体下限，修正算术；限定当前计算图梯度；统一案例阶段与基线；补充 PTQ 例外并统一 GB/GiB 口径 |
| 2026-08-09 | Wiki 自动术语链接、Kimi K2.5 等课程页 | `Agent Swarm` 等复合术语因只登记短词而被拆成半截链接 | 登记完整复合术语并继续采用最长别名优先；增加加粗英文复合术语完整登记检查 |
| 2026-08-09 | D11、公式速查、术语速查 | 把 $W'=W+\alpha BA$ 写成唯一约定，并让“4-bit 存储”看起来像直接 4-bit 算术 | 改用 $W'=W+sBA$，说明常见 $s=\alpha/r$；补充反量化到计算精度与总显存边界 |
| 2026-08-09 | D08、数学急救包、AI Infra | 训练分区、零基索引、softmax、余弦相似度和 TPOT 的边界不够明确 | 补充只用训练分区拟合统计、ID 7 对应第 8 行、概率比、零向量和总生成延迟口径 |
| 2026-08-09 | Kimi/K3、Qwen、GLM 论文课 | 简化递推式被误当 KDA 公式，稀疏度混淆比例与倍数，部分流程图暗示未经证实的训练继承或质量因果 | 改成角色级抽象并链接原式；分开激活/未选比例；移除无证据的检查点继承和“长预算即高质量”表述 |
| 2026-08-06 | 初始课程 | 从原始素材重建 | 建立首版审校口径 |

发现问题时记录：原句、为什么有问题、证据、修订内容和受影响页面。不要静默修改关键事实。
