# D15：确定目标与跑通基线

## 今天完成

1. 按 [实战入口](./) 创建虚拟环境并安装依赖。
2. 运行 `python -m tiny_llm.inspect_env`，保存 Python、PyTorch 和设备信息。
3. 运行全部单元测试。
4. 运行 `baseline`，记录随机权重生成的文本。
5. 写下项目目标：“在演示语料上降低验证交叉熵，并让输出从随机字符变成局部可辨认的中文模式。”

未训练输出很差是正确现象。它是对照组，证明训练后的变化不是模型初始化时就有。

## 今日验收

- [ ] 环境报告能明确显示 CPU 或 CUDA。
- [ ] 所有测试通过。
- [ ] 保存未训练输出和配置名。
- [ ] 没把“会模仿小语料”写成“理解中文”。
