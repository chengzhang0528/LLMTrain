# 动画模型实验室：让一条数据走完一次生成

> **学习导航**：本页承接 D02、D05、D06 和 D07。
>
> 先沿蓝色主线看完一次生成；再扫绿色旁支，检查每一步的数据形态。

## 一张图走完一次生成

```generation-roadmap
{
  "ariaLabel": "单条文字经过 tokenizer、向量表示、prefill、词表打分和候选选择；已接受 ID 进入生成后处理，输出缓冲与生成控制分别处理显示和继续条件，未停止时复用 KV Cache 进入下一轮 decode",
  "learningGoal": "沿一条主线看懂：对象怎样从文字变为 ID、浮点表示、logits，再成为已接受 ID 和可见文字。",
  "watchFor": "粗实线是生成必经阶段；每个绿色分叉点向外展开本阶段注释，不是额外计算步骤；橙色回环只在未触发停止条件时发生。",
  "legend": {
    "main": "必经生成主线",
    "detail": "同源分叉的阶段注释",
    "loop": "满足条件才回到下一轮"
  },
  "stages": [
    {
      "number": "01",
      "title": "文字 → token ID",
      "summary": "tokenizer 按固定配置编码",
      "shape": "单序列：文字 → T_prompt 个整数 ID",
      "side": "left",
      "details": [
        { "label": "输入边界", "value": "聊天应用通常先用 chat template 把消息序列化，再交给 tokenizer" },
        { "label": "规则", "value": "之后可选规范化、预切分；子词、字符或字节编码" },
        { "label": "别混淆", "value": "一个 token 不保证就是一个词" }
      ]
    },
    {
      "number": "02",
      "title": "ID → token Embedding",
      "summary": "本步只查表，得到初始浮点表示",
      "shape": "(1,T_prompt) → (1,T_prompt,D)",
      "side": "right",
      "details": [
        { "label": "Embedding", "value": "ID 只负责定位表中的一行浮点数" },
        { "label": "本步边界", "value": "位置线索不统一归入查表；它在下一步按架构指定的接入点生效" }
      ]
    },
    {
      "number": "03",
      "title": "prefill：整段提示前向",
      "summary": "Attention、MLP、残差与归一化逐层计算",
      "shape": "本图隐藏状态为 (1,T_prompt,D)",
      "side": "left",
      "details": [
        { "label": "位置线索的接入点", "value": "位置 Embedding 可在进入 Block 前与 token 表示结合；RoPE 在每层注意力内作用于 Q/K；相对位置偏置作用于注意力分数" },
        { "label": "并行边界", "value": "本页因果路径中位置可批量计算；因果遮罩仍限制每个位置只能看自己及此前位置" },
        { "label": "临时与跨轮状态", "value": "产生各位置临时隐藏状态，并保存各注意力层后续需要的历史 K/V；工程实现可能分块 prefill" }
      ]
    },
    {
      "number": "04",
      "title": "主干输出 → 词表 logits",
      "summary": "生成算法只需要最后有效位置的词表分数",
      "shape": "主干：(1,T_prompt,D) → 生成所需 logits：(1,V)",
      "side": "right",
      "details": [
        { "label": "对象与需求", "value": "logits 是未归一化分数，不是概率。prefill 主干仍对全部有效提示位置计算隐藏状态 (1,T_prompt,D)；选下一个 token 只需最后有效位置的 (1,V) logits" },
        { "label": "两条可选实现路径", "value": "实现路径 A：全位置过 LM Head 得到 (1,T_prompt,V)，再取最后有效位置。实现路径 B：先取最后有效隐藏状态 (1,D)，再过 LM Head 得到 (1,V)；不与路径 A 重复执行" },
        { "label": "批处理边界", "value": "若 B>1 且使用 padding，各序列最后有效位置不一定是张量末列，必须按有效长度或遮罩定位" }
      ]
    },
    {
      "number": "05",
      "title": "logits → 已接受 ID",
      "summary": "候选规则处理后：argmax 或随机采样",
      "shape": "V 个候选 → 1 个已接受 ID",
      "side": "left",
      "details": [
        { "label": "greedy", "value": "在已应用的约束与分数处理之后，接受最大项" },
        { "label": "sampling", "value": "正 temperature 通常以 logits / temperature 缩放；top-k 通常保留分数最高的 k 项；top-p 将 softmax 概率降序排列，取累计概率达到 p 的最小前缀；最终对保留项重归一化并采样，具体算子及顺序依实现" }
      ]
    },
    {
      "number": "06",
      "title": "已接受 ID → 生成后处理",
      "summary": "记录接受结果；推进输出缓冲并判断是否继续",
      "shape": "已接受 ID 记录可增加 1 项；只有未停止序列才把该 ID 作为下一模型位置；模型参数不变",
      "side": "right",
      "details": [
        { "label": "三种状态", "value": "已接受 ID 记录、下一轮模型输入、可见文本缓冲不是同一个序列；实现可记录 EOS，但它不再作为该序列的下一模型输入，通常也不显示" },
        { "label": "职责边界", "value": "生成控制通常检查 EOS 与长度；停止字符串依赖增量文本缓冲；内容过滤、工具调用和权限属于应用规则，具体模块划分依框架" },
        { "label": "普通推理", "value": "服务通常先从检查点加载权重；请求只读已加载权重，不执行反向传播或参数更新" }
      ]
    }
  ],
  "loop": {
    "source": "同一个已接受 ID：进入生成后处理",
    "display": {
      "label": "文本用途",
      "action": "进入增量反分词 / 文本缓冲",
      "result": "形成完整且允许提交的片段 → 追加可见文字；控制 token 或未完整字节可能使本轮为 0 个可见字符",
      "control": "把依实现从 token 或文本缓冲得到的停止字符串匹配结果交给继续判断"
    },
    "decision": {
      "label": "生成控制",
      "action": "检查该序列是否继续",
      "stop": "EOS 或长度上限；token 或文本缓冲匹配停止字符串；应用规则中止或转交 → 不再进入下一模型前向",
      "continue": "以上均未命中 → 把该 ID 作为下一模型位置，再进入单步 decode 前向"
    },
    "decode": {
      "label": "仅未停止序列",
      "action": "Decode 单步前向",
      "shape": "(1,1,D) + 历史 K/V → (1,V) 原始 logits",
      "detail": "刚接受的 ID 先经 Embedding 查表；位置线索按架构在指定位置作用。模型只计算这个新位置，读取历史 K/V、写入该位置新 K/V，不重新 prefill 整段提示；滑动窗口或缓存淘汰等实现可能限制历史 K/V 的保留长度。KV Cache 不能独自产生 logits。",
      "return": "下一轮原始 logits → 回到第 05 步“logits → 已接受 ID”"
    }
  },
  "boundary": "本图固定批大小 B=1，描述带 KV Cache 的 Decoder-only Transformer 基础自回归路径；T_prompt 是 prefill 提示的有效 token 数，S_cache 是某一时刻缓存的位置数，N_layer 是 Transformer 层数，D 是隐藏宽度，V 是词表大小。生成循环、文本 streamer 与应用层的模块划分依框架，本图表达逻辑职责而非固定软件边界。无缓存实现、状态空间模型、beam search、推测解码或一次接受多个 token 会改变执行形态。"
}
```

**先记三条纠错线：logits 不是概率；KV Cache 不是模型参数；temperature、top-k、top-p 与 greedy 不是四选一。** 不同推理库的采样操作顺序与边界选项可能不同，以具体实现为准。

## 放大某一步

| 卡在哪一步 | 只看这里 |
|---|---|
| 01–02：文字怎样成为向量 | [D02：文字如何变成数字](../01-14天理论课/D02-文字如何变成数字.md) |
| 03–04：模型内部怎样算出 logits | [D05：注意力机制](../01-14天理论课/D05-注意力机制.md)、[D06：完整 Transformer](../01-14天理论课/D06-拼出完整Transformer.md) |
| 05–06：候选怎样成为输出并进入下一轮 | [D07：模型一次运行到底发生什么](../01-14天理论课/D07-模型一次运行到底发生什么.md) |

想看可交互的完整串联，可打开 [Transformer Explainer](https://poloclub.github.io/transformer-explainer/)；它是教学简化，不能直接外推到所有现代模型。

## 本页验收

遮住路线图，用新输入“鸟停在树枝上”闭卷复述：

1. 说出从文字到下一个 token 的至少五个对象，并指出哪一步产生词表 logits。
2. 在启用 sampling、候选 logits 不全相等且只应用正 temperature 时，如果把它从 `0.7` 调到 `1.2`，为什么原始 logits 不变，而分布通常变平？
3. 在未达到缓存上限的追加式 KV Cache 中，为什么已接受 ID 可以先进入记录，但只有未停止序列才把它作为下一模型位置，并在 decode 后增加缓存长度？为什么模型参数始终不增加？

**完成证据**：答案同时包含“对象、动作、形状或序列轴、参数是否更新”。解释不了哪一问，就只回看路线图对应编号。
