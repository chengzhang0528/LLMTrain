# 导数、梯度与学习率

> **使用位置**：D04 的神经网络学习、D09 的训练任务内部循环和 D18 的排错都会用到；学完应能根据梯度正负手算一次更新，并知道更新后必须重新计算 loss。

## 梯度先回答“往哪边会升高”

把参数想成旋钮，把 loss 想成山的高度。梯度告诉我们：每个旋钮轻轻往正方向转时，山高变化得多快。

先代入一组具体数字。旧参数是 `5`，梯度是 `2`，学习率是 `0.1`：

1. 学习率乘梯度：$0.1\times2=0.2$。
2. 从旧参数减去这次改变量：$5-0.2=4.8$。
3. 所以新参数是 `4.8`。

把“旧参数、学习率、梯度、新参数”换成符号，最简单的更新规则写成：

```formula-story
{
  "ariaLabel": "当前参数沿梯度的反方向移动由学习率控制的距离，一阶近似说明足够小步时损失下降",
  "title": "梯度给方向，学习率给步长，负号负责下坡",
  "goal": "先由梯度与学习率组成改变量，再从旧参数减去；一阶近似只保证足够小的局部步长。",
  "coversNext": 3,
  "pattern": "flow",
  "source": { "label": "θₜ=5", "name": "更新前的参数", "detail": "当前位置梯度为 +2，正方向会让 loss 上升", "tone": "blue" },
  "items": [
    { "label": "η∇L=0.1×2=0.2", "name": "算出本次步长", "detail": "梯度定方向与敏感度，学习率缩放幅度", "tone": "orange", "arrow": "缩放梯度" },
    { "label": "Δθ=-0.2", "name": "转向负梯度", "detail": "一阶变化成为 -η||∇L||²", "tone": "rose", "arrow": "加负号" }
  ],
  "result": { "label": "θₜ₊₁=4.8", "name": "完成一次局部更新", "detail": "更新后必须重新前向计算，才知道实际 loss 是否下降", "tone": "green", "arrow": "加到旧参数" },
  "example": { "label": "一阶检查", "text": "梯度 2、步长 -0.2 时，局部预测的 loss 变化约为 2×(-0.2)=-0.4。" },
  "counterfactual": { "label": "改掉符号", "text": "把减号换成加号会局部上坡；拿掉 η 等价于硬用步长 1，可能直接跨过低谷。" },
  "boundary": "负梯度是一阶局部下降方向，不承诺任意步长都下降，也不承诺抵达全局最优；AdamW 还会重标度更新。"
}
```

$$
\theta_{t+1} = \theta_t - \eta\nabla_\theta L
$$

## 负号不是约定出来的

在当前位置附近，光滑损失可以用一阶近似描述：

$$
L(\theta+\Delta\theta)\approx L(\theta)+\nabla L(\theta)^\mathsf{T}\Delta\theta.
$$

选择 $\Delta\theta=-\eta\nabla L(\theta)$ 且 $\eta>0$，代回后得到：

$$
L(\theta+\Delta\theta)-L(\theta)\approx-\eta\lVert\nabla L(\theta)\rVert_2^2\le0.
$$

因此负梯度在**足够小的局部步长**下给出下降方向。这里证明的是一阶近似中的局部性质，不是“任意学习率都下降”，也不保证到达全局最优。

| 改掉什么 | 立即发生什么 | 反向解释 |
|---|---|---|
| 把减号改成加号 | 一阶变化约为 $+\eta\lVert\nabla L\rVert^2$，局部上坡 | 明知坡度向右升高，却还朝右走 |
| 去掉梯度，只减 $\eta$ | 所有参数按同一常数移动，不再依据误差的方向和敏感度 | 梯度为 `-4` 时本该增大参数，固定相减却仍让它变小 |
| 去掉学习率 | 等价于把步长硬设为 1，可能一步跨过低谷 | $L(\theta)=\theta^2,\theta=1$ 时梯度为 2，步长 1 更新到 `-1`，loss 仍是 1 |
| 令 $\eta=0$ | 参数完全不动 | 有梯度也没有实际学习 |

梯度为 0 只表示一阶局部变化消失；它可能是局部最低点、最高点、鞍点或平坦区。

| 符号 | 对象类型 | 本节含义 |
|---|---|---|
| $\theta_t$ | 参数；小例子里是一个数，真实模型中可是一组数 | 第 $t$ 步更新前的参数 |
| $t$ | 整数下标 | 当前训练步编号 |
| $L$ | 标量 | loss；虽然写成大写，仍然是一个数 |
| $\eta$ | 标量 | 学习率 |
| $\nabla_\theta L$ | 与参数形状兼容的梯度 | loss 对参数的局部变化率 |

梯度为负数时，减去负数会让参数增大。这里再次说明：大小写只能提供提示，$L$ 的对象类型由定义决定，不由大写字母决定。

| 当前梯度 | 最简单梯度下降会怎样改参数 | 原因 |
|---:|---|---|
| 正数 | 参数减小 | 减去一个正改变量 |
| 负数 | 参数增大 | 减去负数等于加上正数 |
| 0 | 这一小步不变 | 当前局部斜率为 0，不保证已到全局最好 |

## 学习率控制步长

学习率太大可能跨过低谷并震荡，太小则进展缓慢。真实训练使用 AdamW 等优化器，会利用梯度历史调整每个参数的步幅，但核心仍是根据梯度更新参数。

反向传播不是“模型反着读文本”，而是用链式法则高效计算 loss 对当前计算图中参与运算且允许训练的参数的梯度。冻结参数、未参与本次计算的分支或梯度已断开的参数可能没有梯度。

## 立即自检

旧参数为 `3`、梯度为 `-4`、学习率为 `0.05`，新参数是多少？

<details><summary>核对</summary>

先算改变量：$0.05\times(-4)=-0.2$。再更新：$3-(-0.2)=3.2$。

</details>
