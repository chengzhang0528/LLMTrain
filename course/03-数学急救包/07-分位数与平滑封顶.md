# 分位数与平滑封顶

> **使用位置**：这是一页 Kimi K3 路由与稳定性拓展；学完应能手算平均专家负载，并从数值表看出平滑封顶怎样限制极端值而保留平滑变化。

本页解释 Stable LatentMoE 中两个可用初中数学检查的工具：分位数负载目标和平滑封顶。

## 分位数先回答“排到哪里”

把数从小到大排序：

```formula-story
{
  "ariaLabel": "五个数先排序再按位置选择中位数；路由负载则把全部 token 专家分配平均分给专家",
  "title": "分位数看排序位置，平均负载看总分配怎样均摊",
  "goal": "两者都在定参照，但一个依赖排序位置，一个依赖总量除以专家数，不能混成同一种平均。",
  "coversNext": 2,
  "pattern": "flow",
  "source": { "label": "[8,1,20,4,2]", "name": "未排序的五个观测", "detail": "原出现顺序不决定分位位置", "tone": "blue" },
  "items": [
    { "label": "[1,2,4,8,20]", "name": "从小到大排序", "detail": "中间位置的值 4 是本例中位数", "tone": "orange", "arrow": "按数值排序" },
    { "label": "mk", "name": "路由产生的总分配数", "detail": "m 个 token 每个选择 k 个专家", "tone": "rose", "arrow": "换到负载问题" }
  ],
  "result": { "label": "q=mk/n", "name": "单个专家的理想平均负载", "detail": "把全部分配均摊给 n 个专家", "tone": "green", "arrow": "除以专家数" },
  "example": { "label": "m=8,n=4,k=1", "text": "总分配 8×1=8，均摊到 4 个专家，目标负载为 2。" },
  "counterfactual": { "label": "漏掉 k 或 n", "text": "Top-2 时漏 k 会少算一半；不除 n 得到的是全体专家总分配，不是单个专家目标。" },
  "boundary": "分位数的插值与端点口径可能因库而异；正常 Top-k 要求每个 token 至少选择 1 个专家（1≤k≤n）；理想平均负载也不证明真实 Top-k 路由已经均衡。"
}
```

$$
[1,2,4,8,20].
$$

中位数是 4。更一般地，取 $p\in[0,1]$，$p$ 分位数试图找一个阈值，使大约 $p$ 比例的数据不超过它。不同库对插值和端点的定义可能不同，使用前必须说明口径。

在正常 Top-$k$ MoE 路由中，若一批有 $m\ge0$ 个 token、$n>0$ 个路由专家，每个 token 选择整数个 $k$ 个专家且 $1\le k\le n$，则总分配数为 $mk$，理想平均负载是：

$$
q=\frac{mk}{n}.
$$

例如 $m=8,n=4,k=1$，则每个专家的目标负载 $q=2$。Quantile Balancing 的核心不是“让偏置朝正确方向挪一点”，而是根据路由分数的分位位置直接估计下一步偏置。真实路由仍受并列、批次变化和 Top-$k$ 竞争影响，所以一步更新不等于永远完美均衡。

这里先乘 $mk$，因为每个 token 会产生 $k$ 次专家分配；再除以 $n$，因为理想均衡假设把这些分配平均摊给 $n$ 个专家。若漏掉 $k$，Top-2 时会把总负载少算一半；若不除 $n$，得到的是全体专家总分配，不是单个专家目标。该平均值只定义均衡参照，不证明路由实际均衡。

这里 $m,n,k,q$ 都是数量标量：$m$ 是非负 token 数，$n$ 是正专家数，$k$ 是范围为 $1$ 到 $n$ 的整数，$q$ 是目标平均负载。若只为研究公式边界而令 $k=0$，则 $q=0$ 表示没有发生路由，不属于正常 Top-$k$ 配置。这里的小写 $q$ 与注意力里的 Query 向量没有关系，局部定义优先。

## 公共偏移为什么不改变 Top-k

若所有专家分数同时减去常数 $c$：

```formula-story
{
  "ariaLabel": "两个专家分数共同减去相同常数后差值不变，因此排序和 top-k 选择不变",
  "title": "共同平移不改变相对次序",
  "goal": "Top-k 只依赖谁大谁小；给所有分数加减同一常数，会在两两差值中完全抵消。",
  "coversNext": 1,
  "pattern": "merge",
  "items": [
    { "label": "s₁-c", "name": "专家一共同减 c", "detail": "不是只改某一个专家", "tone": "blue", "arrow": "作差" },
    { "label": "s₂-c", "name": "专家二共同减 c", "detail": "与专家一使用同一常数", "tone": "orange", "arrow": "作差" }
  ],
  "result": { "label": "s₁-s₂", "name": "差值与原来相同", "detail": "正负关系不变，所以相对排序不变", "tone": "green" },
  "example": { "label": "数字", "text": "[9,5] 同减 7 得 [2,-2]，两者差仍为 4，第一名仍是原来的第一名。" },
  "counterfactual": { "label": "只改一项", "text": "若只给 s₁ 减 7，差值就会改变，Top-k 当然可能换人。" },
  "boundary": "该结论保证排序不变；混合权重是否不变还取决于偏置是否参与权重计算。"
}
```

$$
(s_1-c)-(s_2-c)=s_1-s_2.
$$

相对顺序不变，Top-$k$ 选择也不变。因此专家偏置可以整体减去均值进行中心化，而不改变本次排名。

## 平滑封顶

硬截断 $\min(\max(x,-\beta),\beta)$ 会在截断区间外产生零梯度。先固定上界 $\beta=10$ 看几个近似结果：

| 输入 $x$ | 平滑封顶输出（约） | 观察 |
|---:|---:|---|
| 0 | 0 | 小值基本不变 |
| 5 | 4.62 | 开始温和压缩 |
| 10 | 7.62 | 输入达到上界时，输出仍平滑变化 |
| 30 | 9.95 | 大值逐渐贴近 10，但不硬撞到 10 |

把这条“接近 0 时近似原值、很大时逐渐靠近上界”的曲线写成：

```formula-story
{
  "ariaLabel": "输入先除以上界参数决定何时进入 tanh 饱和，再经过 tanh 压到负一和一之间，最后乘同一参数恢复目标上界",
  "title": "同一个 β 做两件相反的事：内部定拐点，外部定上界",
  "goal": "x/β 控制多大输入开始压缩，tanh 提供平滑饱和，外乘 β 把输出范围扩到 (-β,β)。",
  "coversNext": 2,
  "pattern": "flow",
  "source": { "label": "x=30, β=10", "name": "幅值远超目标上界的输入", "detail": "β 必须为正", "tone": "blue" },
  "items": [
    { "label": "x/β=3", "name": "把输入按上界尺度归一", "detail": "决定从什么量级开始进入饱和", "tone": "orange", "arrow": "除以 β" },
    { "label": "tanh(3)≈0.995", "name": "平滑压到 (-1,1)", "detail": "大值仍有连续变化，但逐渐趋平", "tone": "rose", "arrow": "平滑饱和" },
    { "label": "β₁β₂=4×25", "name": "两个封顶分支相乘的幅值界", "detail": "其余门值在 (0,1) 时，上界为 100", "tone": "slate", "arrow": "推广到乘性分支" }
  ],
  "result": { "label": "10×0.995≈9.95", "name": "接近 10 但不硬截断", "detail": "外乘 β 恢复指定的输出幅度", "tone": "green", "arrow": "乘回 β" },
  "example": { "label": "小值", "text": "x 接近 0 时 tanh(x/β)≈x/β，再乘 β 后约等于原 x。" },
  "counterfactual": { "label": "少一个 β", "text": "去掉内除会在 |x|≈1 时过早压扁；去掉外乘则上界固定接近 1，而不是 10。" },
  "boundary": "在两个封顶分支严格落在各自开区间、门值也严格落在 (0,1) 的假设下，输出绝对值严格小于 100；100 是可逼近的极限上界，不表示激活经常等于 100，也不能单独证明训练更稳定或质量更好。"
}
```

$$
\operatorname{softcap}(x,\beta)=\beta\tanh(x/\beta).
$$

其中 $x$ 是输入标量，$\beta$ 是正的幅度上界参数，输出仍是标量。$\tanh$ 是使用的平滑函数；这些字母名称可以改变，但“输入逐渐接近而不硬撞上界”的关系不能改变。

它有两个容易检查的性质：

1. 当 $x$ 接近 0 时，$\tanh(x/\beta)\approx x/\beta$，所以输出约等于 $x$。
2. 因为 $|\tanh(z)|<1$，所以 $|\operatorname{softcap}(x,\beta)|<\beta$。

两处 $\beta$ 分工不同：内部的 $x/\beta$ 决定多大输入开始进入饱和区，外部的 $\beta$ 把 `tanh` 的 `(-1,1)` 输出缩放到 `(-\beta,\beta)`。去掉内部除法时，$\beta=10$ 仍会在 $\lvert x\rvert\approx1$ 附近过早压扁；去掉外部乘法时，上界永远接近 1，而不是设定的 10。令 $\beta\le0$ 也不符合这里“正幅度上界”的定义。

Kimi K3 的 SiTU-GLU 对两个相乘分支分别设置 $\beta_1=4$、$\beta_2=25$。若其余门值在 $(0,1)$ 内，则输出坐标的绝对值满足严格不等式：

$$
\lvert\text{output}\rvert<\beta_1\beta_2=4\times25=100.
$$

所以 $100$ 是可逼近的极限上界，实际值严格小于 100；这不等于实际激活总贴着 100，也不能单独证明训练更好。

## 四级练习

**L1 · 直接应用**：$m=128,n=8,k=1$ 时，目标负载 $q$ 是多少？

<details><summary>参考答案</summary>

$q=128\times1/8=16$。

</details>

**L2 · 变形迁移**：所有专家偏置同时加 7，Top-2 选择是否改变？混合权重是否一定不变？

<details><summary>参考答案</summary>

只要偏置只参与排名，Top-2 不变。混合权重是否不变要看其定义；Kimi K3 报告中的 QB 偏置不进入混合权重，因此该设计下不变，不能推广到所有路由器。

</details>

**L3 · 构造反例**：为什么“各设备分别求中位数，再取平均”不等于“汇总全部样本后求中位数”？

<details><summary>一种构造</summary>

设备 A 为 $[0,0,100]$，中位数 0；设备 B 为 $[101,101,101]$，中位数 101；局部中位数平均为 50.5。全局六个数排序后中间两项是 100 和 101，按常见平均口径全局中位数为 100.5。

</details>

**L4 · 多概念综合**：平滑封顶同时解决了什么，又没有解决什么？

<details><summary>参考答案</summary>

它限制大幅值并保留平滑梯度，降低乘性分支出现极端激活的风险；但它不自动保证损失下降、专家负载均衡、低精度无溢出或最终任务质量更好，这些仍需完整系统和对照实验验证。

</details>

来源：[Kimi K3 技术报告 §2.3 与附录 B–D](https://arxiv.org/abs/2607.24653)。
