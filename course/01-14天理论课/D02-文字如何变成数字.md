# D02：文字如何变成数字

> **学习导航**：承接 D01 的“文本先变成 token 才能进入模型”；本课追踪文字到 token、ID、向量和上下文化表示。学完后，应能指出每一步的对象类型。

> **主线位置**：[模型原理总纲](模型原理总纲.md) -> D01 条件预测 -> **D02 输入表示** -> D03 数学 -> D04 学习机制。本课只解释输入怎样变成可计算对象，不把数字本身说成语义。

## 本课目标

能沿着 `文字 -> token -> ID -> Embedding -> 隐藏状态` 追踪一次表示变化，并区分 token、token ID、词表、Embedding、上下文化隐藏状态和位置信息。

## 先记四次变化

1. **文字 → token：切分边界由 tokenizer 规则决定。**
2. **token → ID：ID 只是词表索引，没有语义大小。**
3. **ID → Embedding：用 ID 查出可计算的浮点向量。**
4. **初始向量 → 隐藏状态：后续网络才把上下文写入表示。**

> **本课范围**：这里只讲文本。图像常先切块，音频常先变成帧或声学特征，再映射为主干可处理的向量；它们不一定共用文本 tokenizer。完整差异见[多模态基础](../06-拓展知识库/多模态基础/)。

## 一条句子的四次变化（第一次必看）

先假设 tokenizer 已经训练和配置完成，词表中有 6 个 token：

| token | ID（程序索引） | 人从第一行开始数 |
|---|---:|---:|
| 我 | 0 | 第 1 行 |
| 爱 | 1 | 第 2 行 |
| 学 | 2 | 第 3 行 |
| 习 | 3 | 第 4 行 |
| 学习 | 4 | 第 5 行 |
| `<eos>` | 5 | 第 6 行 |

**token ID 通常从 0 开始。** 因此 ID `4` 指向人从第一行数起的第 5 行。后文的“查某一行”，都指按这个零起点索引取值。

现在把“我爱学习”送入这个玩具 tokenizer：

1. **字符串**：`我爱学习`。
2. **token 序列**：`我 / 爱 / 学习`。这里共有 3 个位置。
3. **ID 序列**：`[0, 1, 4]`。数字保留 token 的先后，但数字大小不表达语义大小。
4. **查表向量**：分别读取 Embedding 表的索引 0、1、4。

假设每个 token 的 Embedding 只有 3 个数字：

```text
E[0] = [ 0.2, -0.1,  0.7]  # “我”
E[1] = [-0.3,  0.8,  0.1]  # “爱”
E[4] = [ 0.6,  0.4, -0.2]  # “学习”
```

把四个字母和具体数字对应起来：

| 符号 | 表示什么 | 本例数值 |
|---|---|---:|
| `V` | 词表大小 | 6 |
| `B` | 本次同时处理的序列数 | 1 |
| `T` | 每条序列的 token 位置数 | 3 |
| `D` | 每个位置的向量坐标数 | 3 |

ID 张量是 `(B,T) = (1,3)`，值为 `[0,1,4]`。查表后是 `(B,T,D) = (1,3,3)`：3 个位置，各有 3 个浮点数。Embedding 表是 `(V,D) = (6,3)`，若可训练，共有 `6 × 3 = 18` 个参数。

这三个对象的归属也不同：

| 对象 | 在哪里 | 什么时候变化 |
|---|---|---|
| tokenizer 与词表 ID | 模型配套的输入配置 | 通常在训练模型前固定；普通聊天时不会重新学习切分规则 |
| Embedding 表 | 模型参数 | 训练时可被梯度更新；普通推理只读取 |
| 隐藏状态 | 当前请求的中间结果 | 随输入、位置和网络层变化；请求结束后不等于永久写回模型 |

```lesson-board
{
  "ariaLabel": "文字从 token 到 Embedding 的表示链课程总览",
  "eyebrow": "D02 一页总览",
  "title": "数字能计算，不等于数字本身有语义",
  "subtitle": "同一段文字先按具体 tokenizer 切成 token，再用 ID 定位 Embedding 表中的向量；后续网络才让表示结合上下文变化。",
  "panels": [
    {
      "id": "chain",
      "label": "A",
      "title": "同一对象连续换三次形态",
      "subtitle": "每一步都要说清输入与输出",
      "tone": "blue",
      "span": 6,
      "steps": [
        { "title": "文字", "text": "“机器学习”是人可读的字符串" },
        { "title": "token", "text": "例如 “机器 / 学习”；边界由 tokenizer 决定", "tone": "green" },
        { "title": "ID → 向量", "text": "[7, 4] 只定位两行浮点数，不用数字大小计算语义", "tone": "orange" }
      ]
    },
    {
      "id": "granularity",
      "label": "B",
      "title": "为什么常用子词切分",
      "subtitle": "在长度、覆盖和复用之间权衡",
      "tone": "green",
      "span": 6,
      "compare": {
        "headers": ["粒度", "好处", "代价"],
        "rows": [
          { "label": "字符", "values": ["切分单位直观", "序列通常更长；覆盖仍取决于字符集设计"] },
          { "label": "整词", "values": ["单元更接近完整词", "生词处理困难，词表也容易膨胀"] },
          { "label": "子词", "values": ["常见片段可复用", "切分边界未必自然"] }
        ]
      },
      "callout": { "label": "BPE 的作用", "text": "训练 tokenizer 时学习常见片段的合并规则；使用模型时只应用已经固定的规则。", "tone": "orange" }
    },
    {
      "id": "boundary",
      "label": "C",
      "title": "不要把三个数字对象混成一个",
      "subtitle": "它们都和 token 有关，职责却不同",
      "tone": "orange",
      "span": 12,
      "compare": {
        "headers": ["对象", "是什么", "会怎样变化"],
        "rows": [
          { "label": "token ID", "values": ["tokenizer 映射表中的离散索引", "映射配置不变时，token 与 ID 的对应不变"] },
          { "label": "Token Embedding", "values": ["ID 查到的、进入主干前的 token 向量", "可训练时会随训练或检查点而变化"] },
          { "label": "隐藏状态", "values": ["某一层、某一位置在当前上下文中的表示", "会随层、位置和上下文改变"] }
        ]
      },
      "callout": { "label": "检查句", "text": "ID 7 不比 ID 4 “更有语义”；它们只是不同的零起点索引。", "tone": "rose" }
    }
  ],
  "takeaways": [
    { "number": "01", "title": "token 不等于自然语言单位", "text": "不保证刚好是汉字或单词。", "tone": "blue" },
    { "number": "02", "title": "ID 只做索引", "text": "大小不表达重要性。", "tone": "green" },
    { "number": "03", "title": "Embedding 是查表", "text": "读出网络可计算的向量。", "tone": "orange" },
    { "number": "04", "title": "上下文化发生在后续计算", "text": "刚查出的 token 向量尚未汇总其他位置。", "tone": "rose" }
  ],
  "conclusion": "一句话重建：文字按 tokenizer 规则切分，token 被编号，ID 查到向量，网络再让表示结合位置与上下文。",
  "footer": "示例 ID 为教学编号；真实模型使用自己的 tokenizer 与词表。"
}
```

## Tokenizer 用固定规则切分并编号

**排查分词或词表时再读。**

```pencil-flow
{
  "ariaLabel": "文本经过 Tokenizer 得到 token 和 ID，再从 Embedding 表查出向量",
  "learningGoal": "能区分 token、token ID 和 Embedding 向量，并说清 ID 为什么只是查表索引。",
  "watchFor": "盯住同一段文字的表示形式：符号被切块、块被编号、编号再定位一行浮点数。",
  "mode": "animated",
  "nodes": [
    { "id": "text", "label": "机器学习", "x": 86, "y": 116, "width": 128, "tone": "input" },
    { "id": "tokens", "label": "机器 / 学习", "x": 250, "y": 116, "width": 142, "tone": "input" },
    { "id": "ids", "label": "ID [7, 4]", "x": 414, "y": 116, "width": 126, "tone": "process" },
    { "id": "lookup", "label": "查 Embedding", "x": 574, "y": 116, "width": 142, "tone": "process" },
    { "id": "vectors", "label": "两行向量", "x": 704, "y": 116, "width": 104, "tone": "change" }
  ],
  "edges": [
    { "id": "split", "from": "text", "to": "tokens", "label": "切分" },
    { "id": "number", "from": "tokens", "to": "ids", "label": "编号" },
    { "id": "index", "from": "ids", "to": "lookup", "label": "索引" },
    { "id": "read", "from": "lookup", "to": "vectors", "label": "读取" }
  ],
  "steps": [
    { "title": "按固定规则切分", "watch": "看“机器学习”的切分边界落在哪里，字符没有按字面整体进入模型。", "purpose": "应用 tokenizer 已经保存的规范化、切分与子词规则，得到 token。", "detail": "训练 tokenizer 与使用 tokenizer 是两件事：BPE 等方法先从语料学习规则，普通编码时再重复应用固定规则；一个汉字或英文单词都不保证刚好对应一个 token。", "reflection": "如果改成逐字切分，这段文字会从 2 个 token 变成几个？", "active": ["text", "split", "tokens"] },
    { "title": "映射为 ID", "watch": "看 token 标签变成整数 7 和 4，但顺序保持不变。", "purpose": "为每个 token 找到它在词表中的整数编号。", "detail": "ID 只是索引，不表示语义大小或重要程度；编号 7 不比编号 4 更重要。", "reflection": "ID 7 比 ID 4 大，能推出它的语义更强吗？", "active": ["tokens", "number", "ids"] },
    { "title": "用 ID 定位 Embedding", "watch": "看 ID 沿“索引”箭头定位表中的位置；这里没有拿 7 和 4 做大小运算。", "purpose": "把 ID 当作零起点索引，定位 Embedding 表中应该读取的向量。", "detail": "这一步本质上是查表，不是在用 ID 的数值计算语义；ID 7 表示索引 7，也就是人从第一行数起的第 8 行。不同检查点中这个位置保存的浮点数可以不同。", "reflection": "如果词表和 Embedding 表的索引对应错位，模型读到的还会是原 token 的向量吗？", "active": ["ids", "index", "lookup"] },
    { "title": "读出向量", "watch": "看整数 ID 最终变成多维浮点数；这才是后续网络层接收的数值表示。", "purpose": "取出对应行的浮点向量，作为后续网络计算的数值输入。", "detail": "ID 负责索引，查出的向量负责参与连续计算；如果 Embedding 表没有被冻结，训练可以更新其中的浮点数，但不会因此重排 token ID。", "reflection": "训练会更新 token ID，还是更新 ID 指向的向量数值？", "active": ["lookup", "read", "vectors"] }
  ]
}
```

## 表示段运行地图（已能复述四步可跳）

这张流程舞台只放大“输入 → token → ID → Embedding”，并把位置线索作为一条独立支路接入后续计算。`(B,T)` 是整数索引，`(B,T,D)` 才是浮点表示。位置线索不一定直接加到 Embedding 上：RoPE 就作用于注意力层内的 Query 和 Key。舞台上的节点只表示对象类型和因果关系，不代表真实维度或共享坐标空间。

```model-runtime
{
  "ariaLabel": "文字经过 token、ID、Embedding 和位置信息进入模型的表示地图",
  "learningGoal": "能从一个具体序列判断 token、ID、向量和位置分别是什么，并知道它们怎样接到后续 Transformer。",
  "watchFor": "观察对象从离散符号变成整数索引，再变成浮点表示；位置线索与初始表示是两条不同的输入，接入点因架构而异。",
  "checkpoint": { "title": "闭卷重建表示链", "prompt": "按文字、token、ID、Embedding、位置线索、后续注意力的关系点选，说出前三步的形状，并解释位置线索在哪里参与。" },
  "modes": [
    {
      "id": "representation",
      "label": "表示链",
      "overview": "Tokenizer 决定离散切分，ID 只负责查表，Embedding 给出初始浮点表示；位置机制以独立线索接入后续注意力。",
      "rebuild": ["text", "tokens", "ids", "embedding", "position", "attention"],
      "nodes": [
        { "id": "text", "label": "机器学习", "shape": "字符串", "kind": "input", "visual": "message", "visualMeaning": "文本条表示字符串内容，不是坐标空间。", "owner": "应用文本" },
        { "id": "tokens", "label": "机器 / 学习", "shape": "T 个 token", "kind": "represent", "visual": "sequence", "visualMeaning": "离散片段格表示 token 序列，每格是一个单元，不是浮点数。", "owner": "Tokenizer" },
        { "id": "ids", "label": "[7, 4]", "shape": "(B,T) 整数", "kind": "represent", "visual": "sequence", "visualMeaning": "离散 ID 格表示词表索引，编号不是连续语义坐标。", "owner": "词表索引" },
        { "id": "embedding", "label": "查两行向量", "shape": "(B,T,D) 浮点", "kind": "compute", "visual": "tensor", "visualMeaning": "数值张量板中的点是抽样浮点值，真实轴以形状标签为准。", "owner": "Embedding 表" },
        { "id": "position", "label": "位置序列", "shape": "(B,T) 位置索引或相对关系", "kind": "system", "visual": "sequence", "visualMeaning": "位置序列条只标记先后关系，不把位置编号当作距离。", "owner": "位置 Embedding、RoPE 或偏置" },
        { "id": "attention", "label": "后续注意力接入", "shape": "初始表示 + 位置关系", "kind": "compute", "visual": "operation", "visualMeaning": "两块数值板与箭头表示初始表示和位置线索共同进入后续计算，不代表把位置编号当作坐标。", "owner": "Transformer 注意力层" }
      ],
      "edges": [
        { "id": "r1", "from": "text", "to": "tokens", "label": "应用固定规则" },
        { "id": "r2", "from": "tokens", "to": "ids", "label": "编号" },
        { "id": "r3", "from": "ids", "to": "embedding", "label": "按行查表" },
        { "id": "r4", "from": "ids", "to": "position", "label": "记录序列位置" },
        { "id": "r5", "from": "embedding", "to": "attention", "label": "提供初始表示" },
        { "id": "r6", "from": "position", "to": "attention", "label": "提供顺序关系" }
      ],
      "steps": [
        { "title": "文字先变成 token", "watch": "“机器学习”可以切成“机器 / 学习”，切分边界由具体 tokenizer 决定。", "purpose": "把字符串转换成离散单元序列。", "detail": "token 不保证等于汉字、单词或一个可独立解释的词；切分还会影响上下文窗口和成本。", "reflection": "同一句文字换 tokenizer，模型看到的 token 数一定不一样吗？", "active": ["text", "r1", "tokens"], "shape": "字符串 → T 个 token", "payload": { "tokens": ["机器", "学习"], "positions": ["位置 0", "位置 1"], "note": "位置从序列轴记录先后，不由 token ID 的数值决定。" } },
        { "title": "token 变成 ID", "watch": "“机器 / 学习”变成整数 [7, 4]，顺序保留，但数字本身没有语义大小。", "purpose": "用词表中的编号作为查表索引。", "detail": "ID 是离散标签。ID 7 不比 ID 4 更接近某个概念，也不能拿来做连续语义运算。", "reflection": "把所有 ID 都乘 2，会得到同一个模型能理解的新词表吗？", "active": ["tokens", "r2", "ids"], "shape": "(B,T) = (1,2)", "payload": { "tokens": ["机器", "学习"], "ids": ["7", "4"], "positions": ["0", "1"], "note": "这里的数字是索引，不是语义坐标。" } },
        { "title": "ID 查 Embedding 表", "watch": "两个 ID 分别定位表中的两个索引位置；没有把 7 和 4 做大小运算。", "purpose": "把离散输入接入连续的矩阵计算。", "detail": "Embedding 表是参数的一部分；训练会更新索引位置保存的浮点数，但不会把 token ID 改成另一种语义尺度。", "reflection": "如果词表编号和 Embedding 索引错位，模型读到的会是哪一个 token？", "active": ["ids", "r3", "embedding"], "shape": "Embedding: (V,D) → 取出 (B,T,D)", "payload": { "ids": ["7 → 索引 7（第 8 行）", "4 → 索引 4（第 5 行）"], "values": ["[0.2, -0.1, 0.7, …]", "[0.6, 0.4, -0.2, …]"], "note": "真实 D 常为数百到数千，具体取决于模型；省略号表示未绘出的坐标。" } },
        { "title": "位置线索与初始表示共同接入注意力", "watch": "位置序列和 token 向量是不同对象；二者在不同架构指定的位置共同影响后续注意力。", "purpose": "让注意力区分“机器学习”和“学习机器”的顺序与距离。", "detail": "有的模型在进入 Block 前加入位置 Embedding；RoPE 在注意力层里旋转 Query 和 Key；还有方案把相对位置写成注意力偏置。它们都不会改写 token ID，也不能被统一画成“Embedding 变成位置”。", "reflection": "只交换两个 token 的先后而不改变词表映射，哪些位置关系会变化？", "active": ["ids", "r4", "position", "embedding", "r5", "r6", "attention"], "shape": "初始表示 + 位置关系 → 后续注意力", "payload": { "positions": ["位置 0", "位置 1"], "values": ["绝对位置或旋转角", "相对距离或注意力偏置"], "note": "位置线索的接入点取决于架构，不能统一理解成向 Embedding 直接相加。" } }
      ]
    }
  ]
}
```

图中数字只是教学样例。分析真实模型时，要一起查看 tokenizer、词表大小 `V`、隐藏宽度 `D` 和位置方案；不能从一个 ID 猜语义，也不能假定位置机制相同。

> **动画观察提示**：暂停在“ID 查表”，判断变化的是对象、张量形状，还是表中浮点数。

## 训练与使用 tokenizer（选择词表时再读）

真实 tokenizer 可能以字节、字符或子词为基础。一个汉字不保证等于一个 token，一个英文单词也可能被拆成多个 token。上下文窗口按 token 数计算，不按字数计算。

BPE 类方法先从语料学习相邻单元的合并规则，再保存规则。训练模型或聊天时，tokenizer 只应用规则，不会因一句新输入而重训词表。训练方法、语言覆盖和数据关系见 [D08](D08-训练数据与分词器.md)。

同一 tokenizer 通常可以往返编码和解码，但未必原样恢复排版。规范化、空格、未知字符和 `<bos>`、`<eos>` 等特殊 token 都由配置决定。

## Embedding 把 ID 变成可计算的向量

**第一次必看。**

Embedding 是一张可训练的二维表，常记为 `E`。输入 `[0,1,4]`，就依次读取 `[E[0], E[1], E[4]]`。方括号中是从 0 开始的程序索引。

这些数在训练开始时通常没有目标语义。训练会调整它们以改善预测；含义是否接近，还要说明模型、层和度量，不能只看二维投影。

真实 Embedding 往往有数千维。“维”只是坐标数，不表示每一维都有可命名的人类含义。二维、三维例子只为画图和手算。

## Embedding 只是表示旅程的起点

同一个 ID 先查到同一行 Embedding。加入位置并经过 Transformer 后，每个位置会结合上下文形成新的隐藏状态：

`token ID` → 初始 Embedding → 第 1 层隐藏状态 → … → 最后一层隐藏状态

“苹果很好吃”和“苹果发布新品”可从同一行 Embedding 出发，却形成不同隐藏状态。因此“某个词的向量”必须说明模型、层、位置和上下文。

ID 到 Embedding 是查表，不是把整数“升维”。线性投影才用矩阵改变表示，例如生成 Query、Key、Value，或把隐藏状态映射为词表 logits。

数学补充：[高维表示、投影与降维](../03-数学急救包/08-高维表示、投影与降维.md)。

## 顺序从哪里来

**进入 Attention 前必看。**

没有位置线索，注意力无法充分区分先后。原始 Transformer 在网络入口加入正弦位置编码；许多现代 Decoder 在注意力层内用 RoPE，也有架构使用位置偏置。接入点不同，目的都是表示顺序和距离。

## 一个重要边界

Tokenizer 决定离散单元、ID 映射和序列长度。替换它会改变词表与切分；若不同时适配 Embedding 等权重，已有模型就无法按原义读取输入。

## 站内观察

比较页面预设的两种“机器学习”切分：

- 字符级：`机 / 器 / 学 / 习`
- 子词级：例如 `机器 / 学习`

先预测哪种切分会让序列更短，再对照词表大小、序列长度和遇到生词时的表现。不存在对所有语言和任务都最优的唯一切法，也不需要安装或运行分词器。

## 本课验收

<ExerciseBlock
  id="d02-token-id-semantics"
  type="qa"
  question="token ID 为什么不能直接做乘除来表达语义？"
  :concepts="[{ id: 'token-id-embedding', label: 'Token ID 与 Embedding' }]"
  :misconceptions="[{ id: 'token-id-is-meaning', label: '把编号当成语义数值', explanation: 'Token ID 只负责定位词表和 Embedding 行，编号大小不表示含义强弱、远近或倍数。' }]"
  :remediation="{ href: '#tokenizer-用固定规则切分并编号', title: 'Tokenizer 用固定规则切分并编号', reason: '重新沿 token → ID → 查表 → 向量走一遍，分清索引和值。' }"
  answer="token ID 只是词表中的索引编号，编号大小和语义远近没有必然关系。"
  :steps="['Tokenizer 先把 token 映射到一个整数位置。', '这个整数的作用是查找 Embedding 表中的对应行。', '真正进入模型计算的是查出的向量，而不是把 ID 数值本身当作语义大小。']"
  :compare-headers="['token ID', 'Embedding']"
  :compare="[
    { label: '形式', left: '一个整数索引', right: '一排可训练数字' },
    { label: '作用', left: '指出读取哪个索引位置', right: '进入模型计算' },
    { label: '是否可训练', left: '编号规则固定', right: '训练时会更新' }
  ]"
  :flow="['token', 'ID', '查表', '向量']"
  :transfer="{
    question: '把输入序列里的 ID 7 换成 ID 8，但模型权重不变，最直接改变的是什么？',
    options: ['改为读取索引 8 保存的向量', '模型参数量自动翻倍'],
    correct: 'A',
    explanation: 'ID 是索引，换成 8 会读取索引 8 保存的向量，也就是人从第一行数起的第 9 行。'
  }"
  mistake="ID 4 不代表它比 ID 2 更重要、含义是两倍或语义更接近 ID 5。"
/>

<ExerciseBlock
  id="d02-tokenizer-compatibility"
  type="choice"
  question="为什么不能给已有模型随意换一个 tokenizer？"
  :concepts="[{ id: 'tokenizer-vocabulary', label: 'Tokenizer 与词表' }]"
  :misconceptions="[
    { id: 'tokenizer-is-detached', label: '把 Tokenizer 当成可随意替换的外壳', explanation: 'Tokenizer、词表 ID 和模型权重在训练时形成固定对应，替换其中一项会让查表含义错位。', options: ['A', 'C', 'D'] }
  ]"
  :remediation="{ href: '#tokenizer-用固定规则切分并编号', title: 'Tokenizer 用固定规则切分并编号', reason: '重点重看词表 ID 怎样与 Embedding 的索引位置绑定。' }"
  :options="['因为 tokenizer 只支持一种语言', '因为新旧 token ID 与 Embedding 行的对应关系会改变', '因为模型参数量一定会变成零', '因为位置编码会自动删除']"
  correct="B"
  answer="B。模型学到的是旧词表 ID 与 Embedding 行之间的配合，任意替换会打乱这种对应。"
  :steps="['旧 tokenizer 决定了词表、切分和 ID。', 'Embedding 的每一行在训练中适配了旧 ID 所代表的 token。', '新 tokenizer 若让同一 ID 指向别的 token，模型读到的向量含义就会错位。']"
  :transfer="{
    question: '新 tokenizer 让 ID 42 从“猫”变成“合同”，但模型权重不变，模型最先读到什么？',
    options: ['原先为“猫”训练的索引 42 向量', '自动为“合同”重训的新向量', '不再读取 Embedding'],
    correct: 'A',
    explanation: '权重没有重训，索引 42（人从第一行数起的第 43 行）仍保存旧训练形成的表示。'
  }"
  mistake="即使新 tokenizer 看起来更先进，也不能绕过词表和权重的兼容问题。"
/>

<ExerciseBlock
  id="d02-embedding-parameter-count"
  type="calculation"
  question="一个玩具词表有 6 个 token，Embedding 宽度 D=3；本次输入 B=1、T=3。未冻结的 Embedding 表有多少个可训练数字，查表输出形状是什么？"
  :concepts="[{ id: 'embedding-table', label: 'Embedding 表与张量形状', prerequisites: ['tokenizer-vocabulary'] }]"
  :misconceptions="[{ id: 'embedding-count-adds-id', label: '把 ID 也算成可训练参数', explanation: 'ID 是零起点索引；参数只来自词表大小乘以每个向量的维度。' }]"
  :remediation="{ href: '#embedding-把-id-变成可计算的向量', title: 'Embedding 把 ID 变成可计算的向量', reason: '用“行数 × 每行数字数”重新画出玩具 Embedding 表。' }"
  answer="共有 18 个可训练数字，查表输出形状是 (1,3,3)。"
  :steps="['Embedding 表形状是 (V,D)=(6,3)，所以参数数是 6 × 3 = 18。', '本次有 B=1 条序列，每条有 T=3 个 token 位置。', '每个位置查出 D=3 个浮点数，所以输出形状是 (B,T,D)=(1,3,3)。']"
  :transfer="{
    question: 'V=8、D=3、B=2、T=4 时，参数数与查表输出形状是哪一组？',
    options: ['24 与 (2,4,3)', '24 与 (8,3)', '48 与 (2,3,4)'],
    correct: 'A',
    explanation: '参数数由 V × D 得到 24；本次输出形状由 B、T、D 得到 (2,4,3)。'
  }"
  mistake="token ID 本身不算额外参数；V、D 决定表大小，B、T、D 决定本次查表输出形状。"
/>

<ExerciseBlock
  id="d02-contextual-representation"
  type="choice"
  question="同一个‘苹果’ token 出现在‘吃苹果’和‘苹果公司’中，哪项描述最准确？"
  :concepts="[{ id: 'contextual-representation', label: '上下文化表示', prerequisites: ['embedding-table'] }]"
  :misconceptions="[
    { id: 'embedding-equals-hidden-state', label: '把初始 Embedding 当成最终表示', explanation: '同一 ID 的初始查表向量可以相同，但后续隐藏状态会融合位置和上下文。', options: ['A', 'C', 'D'] }
  ]"
  :remediation="{ href: '#embedding-只是表示旅程的起点', title: 'Embedding 只是表示旅程的起点', reason: '对照静态查表向量与经过 Transformer 后的隐藏状态。' }"
  :options="['token ID 和所有层的表示都必须不同', '初始 token Embedding 可以相同，后续隐藏状态会受上下文影响', '只要 token ID 相同，模型最终表示就永远相同', '上下文只改变 tokenizer，不改变网络中的表示']"
  correct="B"
  answer="B。同一个 ID 先读取同一行 token Embedding，但经过位置与上下文计算后，隐藏状态可以不同。"
  :steps="['Tokenizer 先根据词表给 token 分配 ID。', '同一个 ID 会读取同一行初始 token Embedding。', 'Transformer 层会汇入周围位置的信息，因此不同语境中的后续隐藏状态可以分开。']"
  :transfer="{
    question: '同一个“bank”出现在“river bank”和“bank account”里，哪一层最需要把两种含义拉开？',
    options: ['固定 token ID', '上下文化隐藏状态', '文件名'],
    correct: 'B',
    explanation: '上下文进入网络后，隐藏状态才会按语境形成不同表示。'
  }"
  mistake="不要把静态查表得到的 Embedding 与经过多层计算的上下文化隐藏状态混为一谈。"
/>

<ExerciseBlock
  id="d02-positional-information"
  type="choice"
  question="模型为什么需要位置信息？"
  :concepts="[{ id: 'positional-information', label: '位置信息', prerequisites: ['embedding-table'] }]"
  :misconceptions="[
    { id: 'position-is-id-order', label: '把位置机制理解成 ID 排序', explanation: '位置描述 token 在序列中的先后或相对关系，与 token ID 的数值大小无关。', options: ['A', 'C', 'D'] }
  ]"
  :remediation="{ href: '#顺序从哪里来', title: '顺序从哪里来', reason: '用同一 token 放在句首和句尾的例子重新观察位置变化。' }"
  :options="['让 token ID 自动按数值排序', '区分相同 token 出现在不同先后位置', '把所有句子压成同一个向量', '保证模型输出绝对正确']"
  correct="B"
  answer="B。位置信息让模型能够处理先后次序和相对距离。"
  :steps="['单独的 token Embedding 只说明是什么 token。', '同一个 token 放在句首或句尾时，Embedding 查表结果本身相同。', '位置编码或 RoPE 等机制补充它在序列中的位置关系。']"
  :transfer="{
    question: '“狗追猫”和“猫追狗”使用相同三个词时，模型靠什么区分关系？',
    options: ['只看词表大小', '结合 token 表示与位置信息', '把 ID 从小到大排序'],
    correct: 'B',
    explanation: '词相同但顺序不同，位置关系参与后续计算。'
  }"
  mistake="位置机制不是事实校验器，也不保证更长文本一定理解得更好。"
/>

<ExerciseBlock
  id="d02-tokenizer-efficiency"
  type="calculation"
  question="同一句话用 tokenizer A 得到 12 个 token，用 tokenizer B 得到 8 个。B 少用了多少个 token，约减少百分之多少？"
  :concepts="[{ id: 'tokenizer-efficiency', label: 'Tokenizer 序列效率', prerequisites: ['tokenizer-vocabulary'] }]"
  :misconceptions="[{ id: 'shorter-always-better', label: '把单句更短推成全面更好', explanation: '某一句少用 token 只是一条局部证据，还要比较语言覆盖、词表大小、生词和任务效果。' }]"
  :remediation="{ href: '#一个重要边界', title: '一个重要边界', reason: '重新区分“这句话更短”和“Tokenizer 全面更好”两种结论。' }"
  answer="少 4 个 token，相对 A 约减少 33.3%。"
  :steps="['先算数量差：12 - 8 = 4。', '减少比例要以原来的 12 为分母。', '4 ÷ 12 ≈ 0.333，也就是约 33.3%。']"
  :transfer="{
    question: 'A 切出 20 个 token，B 切出 15 个。B 相对 A 约减少多少？',
    options: ['5%', '25%', '75%'],
    correct: 'B',
    explanation: '少 5 个，以原来的 20 为分母，5 ÷ 20 = 25%。'
  }"
  mistake="只能说明这句话在 B 下更短，不能据此断言 B 对所有语言和任务都更好。"
/>

下一课：[D03：够用就好的数学基础](D03-够用就好的数学基础.md)
